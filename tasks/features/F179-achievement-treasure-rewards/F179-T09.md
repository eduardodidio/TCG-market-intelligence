# F179-T09 — Shared-file wiring: CLI registration, Layout mount, README

**Wave:** 2
**Type:** infra
**Depends on:** F179-T05, F179-T07 (and T04/T06 for the README facts)
**Status:** planned
- **Maps to AC:** AC4, AC7, AC9, AC10

## User Story

As a player, I want the achievement toasts to appear anywhere in the app, and
as an operator I want the backfill command available from the main CLI, so that
the feature is actually reachable.

## Objective

Make the minimal edits to the batch-shared files: register the CLI command in
`src/cli/main.py`, mount `<AchievementNotifierHost />` in `Layout.tsx`, and add
the F179 note to `README.md`.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/05-shared-wiring.md`.

- HIGH-RISK SHARED FILES within the F171–F179 batch: keep the diff to the
  listed lines only. Re-read each file right before editing (other features
  may have merged). Don't reformat or reorder imports beyond the added line.
- `main.py` registration goes just before `if __name__ == "__main__":`.
- `Layout.tsx`: `<main ... data-testid="main-content">` is at ~line 449; mount
  the host as a sibling inside the root (router context is available there).
- README: follow the existing feature-note style in `README.md`.

## Implementation details

1. `src/cli/main.py`: `from src.cli.achievement_rewards import backfill_achievement_rewards_cmd  # noqa: E402` + `cli.add_command(backfill_achievement_rewards_cmd)`.
2. `frontend/src/components/Layout.tsx`: import + `<AchievementNotifierHost />`.
3. `frontend/src/components/__tests__/Layout.test.tsx`: add
   `vi.mock("../AchievementNotifierHost", () => ({ AchievementNotifierHost: () => null }))` only if needed.
4. `README.md`: F179 note (tiers, endpoints changes, CLI, toast app-wide, treasure_hunter fix).

## Acceptance criteria

- [ ] `python -m src.cli.main backfill-achievement-rewards --help` works
- [ ] `python -m src.cli.main --help` lists the command; no import cycle
- [ ] Layout renders the host once; Layout tests pass
- [ ] README has the F179 note
- [ ] `cd frontend && npm test && npm run build` green; `pytest tests/` green; `ruff check src/` clean

## Testing

- Backend: `CliRunner().invoke(cli, ["backfill-achievement-rewards", "--help"])`
  test added to the T05 CLI test file (or a 1-test file
  `tests/integration/cli/test_f179_cli_registration.py`).
- Frontend: Layout test asserting host present (with the host mocked to a
  `data-testid` stub) — `cd frontend && npm test -- Layout`.

## Test scenarios

- Happy: `--help` of the command via the main group exits 0.
- Happy: Layout renders stub host exactly once.
- Edge: guest/anonymous Layout render still works (host handles auth itself).
- Error: import of `src.cli.main` doesn't raise (no cycle).
- Boundary: `--dry-run --user-id 999999` against empty temp DB → exit 0.

**Test plan:** ver `F179-test-plan.md` (fixtures: CliRunner, stub de AchievementNotifierHost)

## Diagrams

Verify `docs/diagrams/F179-architecture.mmd` still matches the final wiring
(Layout → Host); update if it doesn't.
