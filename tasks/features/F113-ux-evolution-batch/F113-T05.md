# F113-T05: Project Cleanup (Sujeiras)

**Status:** done
**Wave:** 0
**Request:** R4

## User Story

As a developer, I want to remove inherited placeholder text and leftover
artifacts from the project template, so the codebase looks professional
and doesn't confuse new contributors.

## Dev Notes

### Confirmed Cleanup Targets

#### 1. Stale Git Worktrees (HIGH — disk space)

`.claude/worktrees/` has 10+ abandoned worktree directories:
- `agent-a09fdd48/`, `agent-a0f15cfe/`, `agent-a29e59b5/`, etc.

**Action:** Run `git worktree list` to identify stale entries, then
`git worktree remove <path>` for each. Keep only active worktrees.

#### 2. Unused Agent Prompt Files (MEDIUM)

These came from the claude-didio-config template but are not used by this
project's 4-agent workflow (architect, developer, techlead, qa):

- `agents/prompts/t800.md` — Gandalf meta-agent (config says enabled but CLI doesn't exist)
- `agents/prompts/t1000.md` — Saruman meta-agent
- `agents/prompts/tea.md` — Test Architect
- `agents/prompts/narrative-designer.md` — Narrative Designer
- `agents/prompts/meeting-parser.md` — Meeting parser
- `agents/prompts/_checkpoint-block.md` — Underscore-prefixed variant

**Action:** Delete these files. The 4 active prompts (architect, developer,
techlead, qa) remain.

#### 3. Template Files (KEEP)

These are documentation infrastructure and should remain:
- `tasks/features/FXX-template/` — feature template
- `docs/adr/0000-template.md` — ADR template
- `docs/prd/template.md` — PRD template

**Action:** Keep as-is.

#### 4. Python Lint (LOW)

**Action:** Run `ruff check --select F401 src/` to find unused imports.
Fix any that are clearly dead. Don't go overboard.

### Acceptance Criteria

1. Stale git worktrees removed (only active ones remain)
2. Unused agent prompt files deleted
3. `ruff check src/` passes clean
4. Template files preserved

## Testing

- `git worktree list` shows only active worktrees
- `ruff check src/` passes
- `pytest tests/ -x --timeout=10` still passes (no regressions)
- Template files still exist
