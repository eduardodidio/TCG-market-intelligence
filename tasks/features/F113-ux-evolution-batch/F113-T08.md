# F113-T08: CLAUDE.md + Docs Sync

**Status:** planned
**Wave:** 1
**Request:** R3 (extended)

## User Story

As a developer onboarding to the project, I want CLAUDE.md to have accurate
mission, commands, and project info, so I can get started quickly without
guessing.

## Dev Notes

### CLAUDE.md Updates

1. **Mission** (line ~9): Replace `"TBD — fill in after kickoff."` with:
   ```
   Price intelligence platform for Magic: The Gathering cards in the Brazilian
   market. Collects prices from Liga Magic and MYP Cards, provides portfolio
   tracking, set completion, alerts, and market analytics via a React SPA +
   FastAPI backend.
   ```

2. **Commands** (lines ~16-18): Replace echo placeholders with:
   ```
   - **Build:** `cd frontend && npm run build`
   - **Test (backend):** `pytest tests/ --cov=src --cov-report=term-missing`
   - **Test (frontend):** `cd frontend && npm test`
   - **Run:** `python -m src.cli.main serve`
   - **Lint:** `ruff check src/`
   ```

3. **Architecture** (line ~13): Replace `"No stack assumptions..."` with:
   ```
   - Backend: Python 3.14 + FastAPI + SQLAlchemy + SQLite
   - Frontend: React 19 + TypeScript + Vite + Tailwind CSS
   - Providers: Liga Magic (Playwright), MYP Cards (curl_cffi), Scryfall (bulk data)
   - Deploy: Render (auto-deploy from main branch)
   ```

### Acceptance Criteria

1. No placeholder text remains in CLAUDE.md
2. All commands are accurate and runnable
3. Architecture section reflects actual stack

## Testing

- Manual: verify each command runs correctly
- No automated tests needed (docs-only change)
