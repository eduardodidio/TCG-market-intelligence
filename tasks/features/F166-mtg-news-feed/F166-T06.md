# F166-T06: Tests (backend + frontend)

**Status:** planned
**Wave:** 0

## User Story

As a developer, I want comprehensive tests for the news feed feature.

## Dev Notes

### Files to create
- `tests/api/test_news.py` — API endpoint tests
- `tests/services/test_news_fetcher.py` — fetcher service tests
- `frontend/src/pages/__tests__/NewsPage.test.tsx` — page component tests

### Backend tests (`tests/api/test_news.py`)

1. `test_list_news_returns_items` — basic list with items
2. `test_list_news_empty` — empty state
3. `test_list_news_filter_unread` — only unread items
4. `test_list_news_filter_read` — only read items
5. `test_mark_read_success` — mark as read, verify is_read=true on next list
6. `test_mark_read_idempotent` — marking twice doesn't error
7. `test_mark_unread_success` — unmark read
8. `test_unread_count` — correct count
9. `test_news_requires_auth` — 401 without token
10. `test_category_filter` — filter by category

### Backend tests (`tests/services/test_news_fetcher.py`)

1. `test_fetch_news_parses_rss` — mock feedparser, verify items stored
2. `test_fetch_news_dedup` — same URL not inserted twice
3. `test_fetch_news_error_handling` — bad source doesn't crash
4. `test_categorize_ban_related` — keywords like "ban" map to "ban" category
5. `test_categorize_release` — keywords like "release", "preview" map to "release"

### Frontend tests (`frontend/src/pages/__tests__/NewsPage.test.tsx`)

1. `renders news items` — cards appear
2. `filters by unread/read/all` — tab switching
3. `marks as read on click` — API called + visual update
4. `mark unread button works` — restores unread state
5. `empty state renders` — no items message
6. `shows category badges` — colored category indicators

## Testing

- Backend: pytest with test DB
- Frontend: vitest + @testing-library/react + MemoryRouter
- Mock API calls in frontend tests
