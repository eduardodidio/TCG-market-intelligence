# F98-T06 -- i18n: EN + PT-BR Strings

**Wave:** 2
**Depends on:** T02, T03, T04, T05

## User Story

As a user, all new UI text from F98 should appear in my selected language
(English or Brazilian Portuguese).

## Dev Notes

### New i18n keys

Add to `frontend/src/i18n/locales/en.json` and
`frontend/src/i18n/locales/pt-BR.json`:

#### Scan Summary (T02/T03)

| Key | EN | PT-BR |
|-----|----|-------|
| `scan.summary.title` | Scan Complete | Varredura Concluida |
| `scan.summary.titleWarning` | Scan Complete (with issues) | Varredura Concluida (com problemas) |
| `scan.summary.priced` | Priced | Com preco |
| `scan.summary.notFound` | Not Found | Nao encontrado |
| `scan.summary.rateLimited` | Rate Limited | Limite de requisicoes |
| `scan.summary.otherErrors` | Other Errors | Outros erros |
| `scan.summary.duration` | Completed in {{time}} | Concluido em {{time}} |
| `scan.summary.dismiss` | Dismiss | Fechar |

#### Undo Delete (T04)

| Key | EN | PT-BR |
|-----|----|-------|
| `collection.deleteUndoMessage` | "{{name}}" deleted | "{{name}}" excluido |
| `collection.deleteUndo` | Undo | Desfazer |
| `collection.deleteUndone` | Deletion cancelled | Exclusao cancelada |

#### Inline Edit (T05)

| Key | EN | PT-BR |
|-----|----|-------|
| `inlineEdit.saved` | Saved | Salvo |

**Total: ~12 new keys.**

### Verify no missing keys

Run the i18n key audit (if one exists) or manually grep for all new `t()`
calls introduced by T02-T05 and confirm each key exists in both locale
files.

## Testing

1. `all new keys exist in en.json` -- programmatic check or manual
   verification.
2. `all new keys exist in pt-BR.json` -- same.
3. `no interpolation mismatches` -- keys with `{{name}}` or `{{time}}`
   have matching placeholders in both locales.

## Acceptance Criteria

- [ ] All ~12 new i18n keys present in `en.json`
- [ ] All ~12 new i18n keys present in `pt-BR.json`
- [ ] No missing interpolation variables between locales
- [ ] No hardcoded English strings in T02-T05 components
