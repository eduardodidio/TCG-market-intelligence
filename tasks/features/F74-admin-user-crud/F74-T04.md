# F74-T04 — Full-Stack Tests

**Status:** planned
**Wave:** 1
**Depends on:** F74-T01, F74-T02, F74-T03

## User Story

As a developer, I want comprehensive tests for user CRUD and password expiration flows.

## Dev Notes

### Files to create/modify

1. **`tests/unit/test_admin_user_crud.py`** (NEW)
   - POST /admin/users — happy path (creates user, returns temp password)
   - POST /admin/users — duplicate email (409)
   - POST /admin/users — non-admin (403)
   - DELETE /admin/users/{id} — happy path (sets is_active=0)
   - DELETE /admin/users/{self} — (400)
   - DELETE /admin/users/{nonexistent} — (404)

2. **`tests/unit/test_password_expiration.py`** (NEW)
   - Login with expired password returns password_expired flag
   - Login with valid password (no expiration) works normally
   - /change-password with correct current password → success + new tokens
   - /change-password with wrong current password → 400
   - /change-password clears password_expires_at
   - End-to-end: create user via admin → login → password expired → change → login OK

3. **`frontend/tests/pages/AdminPanel.test.tsx`** (MODIFY)
   - Create user form appears on button click
   - Temporary password shown after successful creation
   - Delete button triggers confirmation
   - Delete calls API and refreshes list

4. **`frontend/tests/pages/ChangePassword.test.tsx`** (NEW)
   - Renders form fields
   - Validates password match
   - Submits and redirects on success

## Testing

- All backend tests pass
- All frontend tests pass
- Integration: admin creates user → user logs in with temp password → forced to change → new password works
