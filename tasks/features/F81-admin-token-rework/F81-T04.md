# F81-T04: Tests for New Credit Flow

**Status:** planned
**Wave:** 0

## User Story
As a developer, I want comprehensive tests covering the new admin credit behavior to ensure no regressions.

## Dev Notes

### Files to Create/Modify
- `tests/credits/test_service.py` — add tests for `claim_monthly_admin_grant`
- `tests/api/routers/test_collection.py` — update tests that relied on admin bypass
- `tests/api/routers/test_scans.py` — update tests that relied on admin bypass
- `tests/api/routers/test_credits.py` — test monthly grant auto-claim on balance check

### Test Scenarios
1. Admin with 0 balance → refresh → 402 (was previously 200 with bypass)
2. Admin with sufficient balance → refresh → balance decreases
3. Admin on 1st of month → GET /balance → receives 10k grant
4. Admin on 2nd call same month → no duplicate grant
5. Non-admin → no monthly grant
6. All users → 12h bonus still works (5 tokens)
7. Admin scan preview → shows real cost (not 0)

## Testing
- All tests use pytest + mocked repo
- Update existing test fixtures that set is_admin=True and expected bypass behavior
