# F81-T01: Remove is_admin Bypass from Credit Guards

**Status:** planned
**Wave:** 0

## User Story
As the system owner, I want admin users to also spend tokens on actions (refresh, scan), so the token economy is consistent and I can track real usage across all users.

## Dev Notes

### Current Admin Bypass Locations
1. `src/api/routers/collection.py:777` — MYP refresh credit guard
2. `src/api/routers/collection.py:891` — MYP refresh deduction
3. `src/api/routers/collection.py:921` — Liga refresh credit guard
4. `src/api/routers/collection.py:1039` — Liga refresh deduction
5. `src/api/routers/scans.py:132` — scan preview cost (shows cost=0 for admin)
6. `src/api/routers/scans.py:163` — scan trigger deduction

### Changes
- Remove all 6 `if not user.is_admin:` guards around credit checks/deductions
- The credit check and deduction should apply to ALL users equally
- Keep `is_admin` field on UserRow — it's still used for admin panel access

### Files to Modify
- `src/api/routers/collection.py` — remove 4 admin bypass blocks
- `src/api/routers/scans.py` — remove 2 admin bypass blocks

## Testing
- Unit test: admin user gets 402 when balance insufficient (was previously bypassed)
- Unit test: admin user balance decreases on refresh
- Unit test: scan preview shows real cost for admin users (not 0)
- Update existing tests that relied on admin bypass behavior
