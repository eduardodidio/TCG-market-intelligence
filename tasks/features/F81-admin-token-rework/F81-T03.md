# F81-T03: Update Seed User + Frontend Admin Credit Display

**Status:** planned
**Wave:** 0

## User Story
As the system owner, I want the seed user to start with 10k tokens (monthly grant), and I want the frontend to show monthly grant info for admin users.

## Dev Notes

### Files to Modify
- `src/cli/main.py` — update `seed-users` command: set initial credits to 10_000 for admin users (was 50)
- `frontend/src/hooks/useCredits.ts` — expose `monthlyGrantAvailable` from balance endpoint
- `frontend/src/components/TreasureBalance.tsx` — show "Monthly grant: 10k" badge for admin users
- `src/api/routers/credits.py` — add `monthly_grant_available` and `next_monthly_grant_at` to balance response
- `frontend/src/i18n/locales/en.json` + `pt-BR.json` — add keys: `credits.monthlyGrant`, `credits.adminMonthly`

## Testing
- Unit test: seed-users creates admin with 10k balance
- Unit test: balance response includes monthly grant info for admins
- Frontend test: TreasureBalance shows monthly grant badge when user is admin
