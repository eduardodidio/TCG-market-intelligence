# F165-T02: Tests for clickable movers

**Status:** completed
**Wave:** 0

## User Story

As a developer, I want tests covering the clickable movers behavior.

## Dev Notes

### Files to modify
- `frontend/tests/components/CollectionMovers.test.tsx` (update existing)

### Changes

1. **Update existing tests** to account for `<Link>` wrapper (rendered as `<a>`)
2. **New test cases**:
   - `mover rows link to card detail page` — verify `href` attribute
   - `gainers link to correct card_id`
   - `losers link to correct card_id`
   - `empty state does not render links`

## Testing

- Use `@testing-library/react` with `MemoryRouter` wrapper
- Assert `<a href="/cards/123">` for each mover row
