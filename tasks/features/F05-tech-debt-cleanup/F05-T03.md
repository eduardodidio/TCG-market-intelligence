# F05-T03 — Parser test coverage (uncovered branches)

**Status:** done
**Wave:** 1
**Type:** test
**Estimate:** medium

## User Story

As a developer, I want `src/parsers/myp.py` to have >= 90% test coverage,
so edge cases in HTML/JSON-LD/JS parsing are validated.

## Dev Notes

### Current gaps (82% coverage, 24 lines missed)

Missing lines: 53-55, 75, 99-100, 108-109, 119, 142, 166, 170-171, 194,
224, 230-231, 238-239, 244-249, 257

### Approach

1. Read `src/parsers/myp.py` to identify which functions/branches the
   missing lines belong to.
2. Add test cases in `tests/unit/test_parsers.py` (or create if not exists)
   for each uncovered branch:
   - Edge cases: empty HTML, malformed JSON-LD, missing fields
   - Missing `precoChartConfig` in history page
   - Cards with no price data
   - Pagination edge cases (no pagination links)
   - Set links edge cases
3. Use minimal HTML strings as fixtures — no need for full page HTML.

## Testing

- `pytest tests/unit/test_parsers.py -v` — all tests pass
- `pytest --cov=src/parsers/myp --cov-report=term-missing` shows >= 90%
- All existing 131 tests still pass
