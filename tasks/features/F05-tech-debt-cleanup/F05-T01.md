# F05-T01 — Housekeeping: .gitignore + status fixes

**Status:** done
**Wave:** 0
**Type:** chore
**Estimate:** trivial

## User Story

As a developer, I want generated/temporary files excluded from git tracking
and feature metadata accurate, so the repo stays clean and trustworthy.

## Dev Notes

1. Add `.coverage` to `.gitignore` (after the `.pytest_cache/` line)
2. Edit `tasks/features/F03-analytics-engine/F03-README.md`: change
   `**Status:** planned` to `**Status:** done`
3. Verify no other stale status fields in F01/F02/F04 READMEs

## Testing

- `git status` should not show `.coverage` as untracked after the change
- Grep all `F0*-README.md` for `Status:` and confirm accuracy
