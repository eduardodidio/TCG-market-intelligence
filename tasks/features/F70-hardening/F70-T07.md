# F70-T07 — Dead Code Removal (ambiguous status + cleanup)

**Status:** done
**Wave:** 2
**Priority:** IMPORTANT
**Estimated effort:** Small
**Review ref:** I05, I06, M04 from full-review-20260827.md

## User Story

As a developer, I need dead code removed so the codebase accurately
reflects current behavior and doesn't mislead future contributors.

## Problem

1. `sync_collection.py:290-308` — unreachable `if match_result.status == "ambiguous"` block (F55 eliminated this status).
2. `match_report.py` — `MatchReportSummary.ambiguous` field and all counting/printing logic for it (matcher never returns "ambiguous").
3. `scans.py:143-144` — unused `_user_id: str = Depends(require_auth_or_api_key)` parameter (immediately followed by `user = Depends(get_current_user)`).
4. `collection.py:72` — `except (ValueError, Exception)` redundancy (Exception covers ValueError).

## Dev Notes

### sync_collection.py

Remove lines 290-308 (the `elif match_result.status == "ambiguous":` block).
Update `SyncSummary` if it has an `ambiguous` counter.

### match_report.py

- Remove `ambiguous` field from `MatchReportSummary` dataclass
- Remove all `summary.ambiguous +=` counting
- Remove `ambiguous` from JSON output and print statements
- Keep backward compat: if any external code reads the JSON, add migration note

### scans.py

Remove unused `_user_id` parameter from scan trigger endpoint.

### collection.py

Change `except (ValueError, Exception):` to `except Exception:` (or better,
use specific types like `except (ValueError, TypeError):`).

### Files to modify

- `src/collectors/sync_collection.py` — remove ambiguous block
- `src/collectors/match_report.py` — remove ambiguous field + logic
- `src/api/routers/scans.py` — remove unused param
- `src/api/routers/collection.py` — fix exception handler

## Testing

- All existing tests must pass (no behavior change)
- `tests/collectors/test_sync_collection.py` — remove/update any ambiguous tests
- `tests/collectors/test_match_report.py` — remove ambiguous assertions

## Acceptance Criteria

- [ ] No reference to "ambiguous" status in sync_collection or match_report
- [ ] No unused parameters in scan router
- [ ] No redundant exception types
- [ ] All tests pass
- [ ] `grep -r "ambiguous" src/` returns no hits (except comments if any)
