# F174-T01 — Branch check + PRD

**Wave:** 0
**Type:** docs / infra
**Depends on:** —
**Status:** done
- **Maps to AC:** AC9

## User Story

As a product owner, I want a PRD for aligning Trades with Minha Coleção, so that every later task implements an agreed scope.

## Objective

Confirm that work happens on `homol` (or the orchestrator worktree cut from it, e.g. `wt/F174`, which is valid; do not switch branches), then write the F174 PRD.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/04-docs.md`.

- Gitflow (CLAUDE.md): feature work happens on `homol`, never on `main`. Run
  `git branch --show-current`. If it is `main`, stop and report. If it is
  `homol` or an orchestrator worktree branch (e.g. `wt/F174`, `wt/*`, `claude/*`,
  `didio/*`), proceed. `wt/F174` is the expected, VALID branch for this batch. Do NOT create or switch branches yourself when running inside a worktree.
- PRD template: `docs/prd/template.md`. Existing examples: `docs/prd/F16-explore-cards-sorting.md`.
- The PRD content comes from `_brief/00-overview.md` (problem, scope, ACs) and
  `_brief/04-docs.md` (PRD section: personas, risks).

## Implementation details

- Create `docs/prd/F174-trades-collection-filters.md` with: Problem, Personas,
  Goals / Non-goals, Functional requirements (AC1–AC9), API changes table
  (4 endpoints: 2 extended, 2 new), UX notes, Risks & mitigations, Out of scope.

## Acceptance criteria

- [ ] Current branch verified (not `main`) and noted in the task summary.
- [ ] `docs/prd/F174-trades-collection-filters.md` exists and follows the template sections.
- [ ] The PRD lists all 4 endpoint changes with params and defaults.
- [ ] The PRD lists the "do not touch" shared files from `_brief/00-overview.md`.

## Testing

- Docs-only task, so there are no automated tests.
- Validate that the markdown renders (headings, tables) and that all internal links are relative.
- Check the branch: `git branch --show-current` must print `homol` or an orchestrator worktree branch (`wt/*`, `claude/*`, `didio/*`), never `main`.
- Sanity guard (no code touched): `ruff check src/` stays green.

## Test scenarios

- Happy: the PRD is complete, and every AC maps to a later task.
- Edge: the branch is `main` → stop and report (no file written).
- Error: the template is missing → use the section list above.
- Boundary: the PRD stays within ~150 lines (concise).

## Diagrams

None (diagrams are owned by T13).
