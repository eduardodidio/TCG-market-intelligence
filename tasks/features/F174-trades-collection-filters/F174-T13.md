# F174-T13 — Diagrams: F174 architecture + user journey

**Wave:** 3
**Type:** docs
**Depends on:** F174-T07, F174-T08, F174-T09, F174-T10, F174-T11, F174-T12
**Status:** planned

## User Story

As a future developer, I want diagrams of how the trade filters flow from UI to DB, so that I can extend them safely.

## Objective

Create `docs/diagrams/F174-architecture.mmd` and `docs/diagrams/F174-journey.mmd`, reflecting the code as shipped.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/04-docs.md`.

- Templates: `docs/diagrams/templates/architecture.mmd`, `docs/diagrams/templates/user-journey.mmd`.
  Examples: `docs/diagrams/F168-architecture.mmd`, `docs/diagrams/F168-journey.mmd`.
- The content spec is in `_brief/04-docs.md`. Before drawing, read the actual shipped files (T03–T12) and
  draw what exists, not what was planned.
- Keep the Mermaid syntax valid (no parentheses in unquoted labels. Quote labels with `?`, `/`, `&`).

## Implementation details

- `docs/diagrams/F174-architecture.mmd` (new)
- `docs/diagrams/F174-journey.mmd` (new)
- Optionally add both to the index in `docs/diagrams/README.md` **only if** that file lists per-feature diagrams (append a line, and do not reorder).

## Acceptance criteria

- [ ] Both files exist and parse (validate with `npx -y @mermaid-js/mermaid-cli` if available offline, otherwise check them carefully by eye).
- [ ] Architecture shows the shared kit, 4 pages, 2 API clients, 2 routers, `TradeQueries` and the DB tables, with labelled endpoint edges.
- [ ] Journey shows swimlanes, the server- vs client-side decision, the empty-result path and the error/retry path.

## Testing

Docs-only. Parse-check the Mermaid. No automated tests.

## Test scenarios

- Happy: the diagrams render in GitHub.
- Edge: long labels are wrapped with `<br/>`.
- Error: a syntax error → fix it before finishing.
- Boundary: diagrams match the shipped code (e.g. if T12 was reverted, don't draw MyCollection → CardFilterBar).

## Diagrams

Owner of `F174-architecture.mmd` and `F174-journey.mmd`.
