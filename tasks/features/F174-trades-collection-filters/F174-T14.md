# F174-T14 — README.md: F174 delivery note

**Wave:** 3
**Type:** docs
**Depends on:** F174-T07, F174-T08, F174-T09, F174-T10, F174-T11, F174-T12
**Status:** done
- **Maps to AC:** AC9

## User Story

As a project user, I want the README to mention the new trade filters and endpoints, so that I know what changed.

## Objective

Append a short F174 note to the project `README.md` (new/extended endpoints and the trade UI alignment).

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/04-docs.md`.

- `README.md` is a **high-conflict file across the F171–F179 batch**. This task is the only F174 edit to it.
  Keep it append-only, a few lines, and placed in the section where other features log deliveries (read the
  file and match the existing format and language).
- The text is in `_brief/04-docs.md` → README.md. Confirm the endpoint names against the shipped routers
  (`src/api/routers/marketplace.py`, `src/api/routers/trade_match.py`).

## Implementation details

- `README.md` (append-only edit)

## Acceptance criteria

- [ ] README mentions: trade pages use the collection filter bar/grid, the 2 new endpoints and the 2 extended endpoints with their params.
- [ ] `git diff README.md` shows only added lines.

## Testing

- Docs-only. Verify the markdown renders.
- Verify the endpoint paths match the code: `grep -n 'listings/sets\|duplicates/sets' src/api/routers/*.py`.
- Sanity guard: `ruff check src/` stays green (no code touched).

## Test scenarios

- Happy: the note is added in the right section.
- Edge: README has no changelog section → add the note under the most relevant features/API section.
- Error: the endpoint names differ from the code → use the code's names.
- Boundary: ≤ 10 added lines.

## Diagrams

None.
