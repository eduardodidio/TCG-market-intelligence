# F174-T12 — MyCollection adopts `CardFilterBar` (zero regression)

**Wave:** 2
**Type:** frontend
**Depends on:** F174-T05
**Status:** planned
- **Maps to AC:** AC1, AC7, AC8

## User Story

As a maintainer, I want Minha Coleção and Trocas to share one filter bar component, so that future filter changes apply everywhere consistently.

## Objective

Replace MyCollection's sticky container + search/sort row + SetIconFilter with `<CardFilterBar>`, passing the remaining rows as children, with no behaviour change.

## Dev Notes

Veja `_brief/00-overview.md`, `_brief/02-frontend-kit.md` e `_brief/03-trade-pages.md`.

- Target block: `frontend/src/pages/MyCollection.tsx` ~L751–L1060 (starts at the comment
  `{/* Search, sort, and filters — sticky bar */}`).
- Map: `search=searchTerm`, `onSearchChange=setSearchTerm`, `sortOptions=COLLECTION_SORT_OPTIONS`,
  `sortValue={`${sortBy}-${sortDir}`}`, `onSortChange=handleSortChange`, `setOptions`,
  `selectedSet`, `onSetSelect=setSelectedSet`. **Do NOT pass `gridSize`**. The acquisition chips,
  mobile actions row (`sm:hidden`) and desktop actions row (`hidden sm:flex`) move **unchanged** into `children`.
- Remove the now-unused `SearchBar`/`SetIconFilter` imports only if nothing else in the file uses them
  (grep first). `SortSelect` import: keep `COLLECTION_SORT_OPTIONS`.
- **Guard: do not edit any existing test file.** All of `pages/__tests__/MyCollection*.test.tsx`,
  `CollectionCardTile3D.test.tsx` and the other page tests must pass as is.
- This is the only MyCollection change in F174. Keep the diff minimal.

## Implementation details

- `frontend/src/pages/MyCollection.tsx` (JSX restructuring of the filter bar only, with the imports adjusted)

## Acceptance criteria

- [ ] The rendered DOM keeps `sticky-filter-bar`, `sort-select`, `set-icon-filter`, `acquisition-filter`, `mobile-actions-toggle`, `select-mode-btn` etc.
- [ ] No state, effect or fetch logic changed (diff limited to the filter-bar JSX + imports).
- [ ] All pre-existing frontend tests pass with **no test file modified**.
- [ ] `npm run build` passes (no unused-import TS errors).

## Testing

- Run the full `cd frontend && npm test` and `cd frontend && npm run build`.
- Existing MyCollection tests must pass unmodified: `cd frontend && npx vitest run src/pages/__tests__/MyCollection` (the pre-existing `tests/pages/MyCollection.test.tsx` failure is baseline, not a regression).
- Optionally add `pages/__tests__/MyCollectionFilterBar.test.tsx` (new file) asserting that the `CardFilterBar` container testid is present and that the acquisition chips are inside it.
- Backend guard for AC8: `pytest tests/ --cov=src --cov-report=term-missing` and `ruff check src/` stay green.

## Test scenarios

- Happy: search/sort/set behave exactly as before (the existing tests prove it).
- Edge: a collection with no sets → no set row (same as before).
- Error: build fails due to unused imports → remove them.
- Boundary: mobile (sm:hidden row) and desktop (hidden sm:flex row) are both still rendered inside the bar.

## Diagrams

The `MyCollection → CardFilterBar` edge in `F174-architecture.mmd` (T13).
