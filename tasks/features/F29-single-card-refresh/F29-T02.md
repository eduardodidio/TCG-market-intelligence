# F29-T02: Frontend API client function

**Status:** planned
**Wave:** 1

## User Story

As a frontend developer, I need a `refreshCardPrice(entryId)` function to call
the new refresh endpoint.

## Dev Notes

### Files to modify
- `frontend/src/api/collection.ts` — add `refreshCardPrice` function

### Implementation
1. Add `refreshCardPrice(entryId: number, params?: Record<string, string>)` function
2. Uses `apiPost<CollectionCardDetail>(`/api/v1/collection/${entryId}/refresh`, {})`
3. Accept optional `currency` param to get back converted price
4. Longer timeout (30s) since MYP fetch can be slow

## Testing

- Unit test: verify correct URL and method
- Unit test: verify auth header included
