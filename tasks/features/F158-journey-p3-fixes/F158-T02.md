# F158-T02: Login OAuth section cleanup

**Feature:** F158 — Journey P3 Polish
**Wave:** 0
**Status:** planned

## User Story

As a user on the login page, I don't want to see disabled OAuth buttons
that suggest functionality that doesn't exist, as it creates confusion.

## Dev Notes

### Current State

`Login.tsx` lines 183-214: Three disabled buttons (Google, Microsoft, Apple)
with `cursor-not-allowed opacity-50`. They serve no purpose and clutter the UI.

### Implementation

Remove the entire OAuth placeholder section (lines 183-214). The "or continue
with" divider and disabled buttons add visual noise without value.

### Files
- `frontend/src/pages/Login.tsx` — remove OAuth placeholder section

## Testing

- Unit: OAuth buttons no longer render on login page
- Unit: login form still works normally

## Acceptance Criteria

- [ ] No disabled OAuth buttons on login page
- [ ] Login/register flow unchanged
