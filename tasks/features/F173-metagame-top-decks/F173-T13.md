# F173-T13 — Abas "Meus decks | Mercado" no TopDecksPage + link no TopDecksPreview

- **Wave:** 3
- **Type:** frontend
- **Depends on:** F173-T11
- **Status:** planned
- **Maps to AC:** AC5, AC8

## User Story
As a player on `/decks/ranking`, I want a "Mercado" tab with a format filter, so that I
can switch between my decks' ranking and the metagame top decks.

## Objective
Integrar `MetaDecksPanel` na página existente via query param, sem rota nova.

## Dev Notes
Veja `_brief/00-overview.md` e `_brief/04-frontend.md` (seção Integração).
- `frontend/src/pages/TopDecksPage.tsx` usa `useSearchParams` (`sort_by`, `period`).
  Adicionar `view` (`mine` default | `meta`) e `format` (default `commander`).
  Com `view=meta`, ocultar sort/period (não se aplicam) e renderizar `<MetaDecksPanel>`.
  `view=mine` deve manter exatamente o comportamento atual (inclusive fetch —
  não chamar `fetchDeckRanking` quando `view=meta`).
- Breadcrumb: manter; título muda para `t("metaDecks.title", {defaultValue:"Top Decks do Mercado"})` na aba meta.
- `frontend/src/components/TopDecksPreview.tsx` (usado em `MarketPage.tsx:91`): adicionar
  link `data-testid="top-decks-preview-meta-link"` → `/decks/ranking?view=meta` no header.
- **NÃO editar `App.tsx`/`Layout.tsx`.** i18n via `defaultValue` (chaves em T15:
  `topDecks.tabMine`, `topDecks.tabMeta`, `topDecks.viewMeta`).
- Não existe teste do TopDecksPage hoje — criar `frontend/src/pages/__tests__/TopDecksPage.test.tsx`
  (padrão de `DeckBuildWizard.test.tsx`). Verificar se há teste de TopDecksPreview/MarketPage
  que quebre com o link novo (`grep -rn "TopDecksPreview\|top-decks-preview" frontend/src --include=*.test.tsx`).

## Implementation details
- Tabs `data-testid="topdecks-tab-mine"` / `"topdecks-tab-meta"` com `aria-selected`.
- Trocar aba preserva outros params; trocar formato atualiza `format` no URL.

## Acceptance criteria
- [ ] `/decks/ranking` sem params = comportamento atual.
- [ ] `/decks/ranking?view=meta&format=modern` renderiza painel com formato modern.
- [ ] Deep-link funciona (refresh mantém aba e formato).
- [ ] Link no preview presente. Testes existentes continuam verdes; `npm run build` ok.

## Testing
- Vitest + Testing Library + `MemoryRouter initialEntries`; mock de `../../api/deckRanking` e
  de `../../components/meta/MetaDecksPanel` (stub que exibe o `format` recebido).
- `cd frontend && npx vitest run src/pages/__tests__/TopDecksPage.test.tsx`
- Regressão: `cd frontend && npm test` (inclui testes de TopDecksPreview/MarketPage).

## Test scenarios
- Happy: default mostra ranking do usuário (fetchDeckRanking chamado).
- Happy: clicar aba Mercado → URL `view=meta`, painel renderizado, fetchDeckRanking não chamado de novo.
- Edge: `view=meta` sem `format` → commander. `format` inválido na URL → commander.
- Edge: onFormatChange do painel atualiza `format` na URL.
- Error: `view=xyz` → trata como `mine`.
- Boundary: preview com decks mostra link; preview sem decks continua retornando null.

## Diagrams
Nenhum (T14 cobre a jornada).

**Test plan:** ver `F173-test-plan.md` (fixtures: none)
