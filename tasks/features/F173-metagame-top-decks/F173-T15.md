# F173-T15 — Integração em arquivos compartilhados (app.py, cli/main.py, i18n, README, .gitignore)

- **Wave:** 4
- **Type:** infra
- **Depends on:** F173-T10, F173-T12, F173-T13, F173-T14
- **Status:** planned
- **Maps to AC:** AC2, AC4, AC5, AC7, AC8, AC9

## User Story
As the batch orchestrator, I want all F173 edits to shared files isolated in one tiny
final task, so that parallel features F171–F179 merge without conflicts.

## Objective
Registrar router e comando, adicionar chaves i18n, ignorar cache, atualizar README.

## Dev Notes
Veja `_brief/05-integration-and-conflicts.md` (lista exata) e `_brief/04-frontend.md`.
- **Faça `git pull`/rebase local da worktree sobre `homol` antes** (sem rebase em
  branches compartilhadas; se houver conflito com outra feature, resolver mantendo ambas as linhas).
- `src/api/app.py`: imports de routers ficam dentro da função de criação do app, perto de
  `app.include_router(news_router, prefix="/api/v1")` (~linha 391). Adicionar import +
  `app.include_router(meta_decks_router, prefix="/api/v1")` logo após o de news.
- `src/cli/main.py`: no fim do arquivo (antes de `if __name__ == "__main__":` se existir):
  `from src.cli.metagame import collect_metagame_cmd  # noqa: E402` + `cli.add_command(collect_metagame_cmd)`.
  Checar ciclo de import: `src/cli/metagame.py` não pode importar `src.cli.main`.
- i18n: `frontend/src/i18n/locales/pt-BR.json` e `en.json` — novo bloco top-level
  `"metaDecks"` com as chaves listadas no comentário do topo de
  `frontend/src/components/meta/MetaDecksPanel.tsx` (T11) + `topDecks.tabMine`,
  `topDecks.tabMeta`, `topDecks.viewMeta` (T13). Valores pt-BR = os `defaultValue`.
  Rodar o teste de paridade de chaves em `frontend/src/i18n/__tests__/` se existir.
- `.gitignore`: adicionar `data/cache/` (hoje não está).
- `README.md`: seção "Top Decks do mercado (F173)": endpoints `GET /api/v1/meta-decks/formats`,
  `GET /api/v1/meta-decks?format=`, `GET /api/v1/meta-decks/{id}`; comando
  `python -m src.cli.main collect-metagame [-f modern] [--limit 20] [--dry-run]`;
  `bats/collect-metagame.bat` (semanal); UI `/decks/ranking?view=meta`; fontes + link ADR.
- Stage arquivo por arquivo (nunca `git add -A`/`.`).

## Implementation details
6 arquivos, mudanças mínimas descritas acima. Nenhuma lógica nova.

## Acceptance criteria
- [ ] `python -m src.cli.main collect-metagame --help` funciona.
- [ ] `create_app()` expõe `/api/v1/meta-decks` (teste abaixo).
- [ ] Nenhuma chave i18n faltando (sem fallback de `defaultValue` em pt-BR/en).
- [ ] Suite completa verde: `pytest tests/ --cov=src --cov-report=term-missing`,
      `ruff check src/`, `cd frontend && npm test && npm run build`.

## Testing
- Adicionar `tests/api/test_meta_decks_registration.py`: `TestClient(create_app())` (ver
  `tests/api/test_app.py` p/ como instanciar) → `GET /api/v1/meta-decks/formats` → 200.
- Adicionar `tests/cli/test_cli_metagame_registration.py` (arquivo novo; não editar o de T12):
  `CliRunner().invoke(cli, ["collect-metagame", "--help"])` exit 0.
- `pytest tests/api/test_meta_decks_registration.py tests/cli/test_cli_metagame_registration.py -q`
- Suite completa: `pytest tests/ --cov=src --cov-report=term-missing`, `ruff check src/`,
  `cd frontend && npm test && npm run build`.

## Test scenarios
- Happy: rota registrada responde; comando aparece em `cli --help`.
- Edge: chaves i18n en/pt-BR com mesma estrutura.
- Error: formato inválido via app real → 422 no envelope padrão de erro.
- Boundary: suite inteira + build verdes (regressão das outras features).

## Diagrams
Nenhum (confirmar links do README para `docs/diagrams/F173-*.mmd`).

**Test plan:** ver `F173-test-plan.md` (fixtures: none)
