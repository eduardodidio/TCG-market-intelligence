# F96-T06: i18n Keys for All New Strings (EN + PT-BR)

**Wave:** 0
**Depends on:** none

## User Story

As a developer working on T01-T05, I want all new i18n keys pre-defined in
both locale files, so I can reference them immediately without coordination.

## Dev Notes

### Files to modify

1. `frontend/src/i18n/locales/en.json`
2. `frontend/src/i18n/locales/pt-BR.json`

### New keys to add

Under the `"credits"` namespace:

| Key | EN | PT-BR |
|-----|-----|-------|
| `credits.earnInfo` | "Earn 5 tokens every 12 hours by claiming your bonus." | "Ganhe 5 tokens a cada 12 horas resgatando seu bonus." |
| `credits.claimBonusModal` | "Claim Bonus" | "Resgatar Bonus" |
| `credits.bonusClaimed` | "+{{amount}} tokens!" | "+{{amount}} tokens!" |
| `credits.costBadge` | "({{cost}} token)" | "({{cost}} token)" |
| `credits.refreshCostTooltip` | "Costs {{cost}} token to refresh" | "Custa {{cost}} token para atualizar" |
| `credits.approxCost` | "~{{count}} tokens" | "~{{count}} tokens" |

Under the `"schedules"` namespace:

| Key | EN | PT-BR |
|-----|-----|-------|
| `schedules.creditPauseTooltip` | "Paused: insufficient tokens. Claim your bonus or wait for monthly grant to resume." | "Pausado: tokens insuficientes. Resgate seu bonus ou aguarde o credito mensal para retomar." |
| `schedules.creditPauseHint` | "Claim bonus or add tokens to resume" | "Resgate bonus ou adicione tokens para retomar" |

### Implementation

- Add keys alphabetically within their namespace.
- No duplicate keys -- verify none of these already exist.
- Use `{{variable}}` interpolation syntax (react-i18next standard).

## Testing

- Test: all new keys resolve (no missing key warnings) in EN locale.
- Test: all new keys resolve in PT-BR locale.
- Can be verified via a simple unit test that imports both locale files and
  checks for key presence, or via existing i18n coverage tests.

**Estimated new tests:** 2 (one per locale, or a parameterized test)
