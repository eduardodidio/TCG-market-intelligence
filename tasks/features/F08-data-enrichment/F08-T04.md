# F08-T04: End-to-end validation, documentation, and README update

**Wave:** 2
**Status:** done
**Estimate:** M

## User Story

As a project maintainer, I want the F08 changes validated end-to-end and
documented, so that the project documentation stays current and the feature
is considered complete per project standards.

## Dev Notes

### End-to-End Validation

1. Start the backend: `make serve`
2. Start the frontend: `cd frontend && npm run dev`
3. Verify the following manually:
   - Dashboard KPIs reflect expanded dataset (more cards, sets, observations)
   - Dashboard movers section shows non-zero price changes
   - Cards page shows cards from multiple sets with correct Portuguese names
   - Card Detail page shows price chart for a card with accented name
   - Market Movers page works with 30d default
   - Search works with accented characters (e.g., search "magica")

### Documentation Updates

**Diagrams** (update or create under `docs/diagrams/`):
- `F08-architecture.mmd` -- data flow showing encoding fix point in the
  pipeline (provider -> parser -> DB -> API -> frontend)
- `F08-journey.mmd` -- user journey for viewing corrected data on dashboard

**README.md** (project root):
- Add F08 entry noting: encoding fix, movers tuning, collection expansion
- Update any data statistics mentioned in README

**MEMORY.md** updates:
- Update "Status" section with F08 completion
- Update DB stats (card count, set count, observation count)
- Note the encoding fix for future reference

### Run Full Test Suites

```bash
# Backend
python -m pytest --tb=short

# Frontend
cd frontend && npm run test
```

All tests must pass. No regressions.

## Acceptance Criteria

- [ ] Manual end-to-end walkthrough completed (all 4 pages verified)
- [ ] `docs/diagrams/F08-architecture.mmd` created
- [ ] `docs/diagrams/F08-journey.mmd` created
- [ ] Project `README.md` updated with F08 delivery notes
- [ ] All backend tests pass
- [ ] All frontend tests pass
- [ ] No encoding issues visible in API responses or frontend

## Testing

- Full backend pytest suite
- Full frontend Vitest suite
- Manual walkthrough of all 4 dashboard pages
- API spot checks with curl for encoding correctness
