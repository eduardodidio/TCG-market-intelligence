# F178-T03 — Read-only `GET /api/v1/news/status`

- **Wave:** 1
- **Type:** backend
- **Depends on:** F178-T01
- **Status:** done
- **Maps to AC:** AC5, AC6

## User Story
As a reader of the News page, I want to know when news was last collected, so that I can tell the difference between "no news" and "stale data".

## Objective
Add `Repository.get_news_status()` and a `GET /news/status` route; enforce that the news router does no network I/O.

## Dev Notes
Veja `_brief/00-overview.md` e `_brief/03-status-endpoint.md`.
- Repository method goes inside the `# ── News feed (F166)` section of `src/database/repository.py` (~line 5075), right after `upsert_news_item`. Don't touch other sections (batch features edit this file too).
- Envelope: `success_response(...)` from `src.api.schemas.envelope`, `ApiResponse[...]` response model — same as sibling routes.
- Auth: `Depends(get_current_user)` like other news routes.
- Do not edit `src/api/app.py` — router already included under `/api/v1`.

## Implementation details
- `Repository.get_news_status() -> dict` single aggregate query; ISO strings or `None`.
- `NewsStatusResponse` pydantic model + `@router.get("/status")` handler in `src/api/routers/news.py`.

## Acceptance criteria
- [ ] Empty DB → `{"total_items": 0, "last_fetched_at": null, "newest_published_at": null}`.
- [ ] Populated DB → correct count and max timestamps.
- [ ] Unauthenticated request rejected like other news endpoints.
- [ ] Test asserts `news.py` source contains no `news_fetcher`, `httpx`, `feedparser`, `requests` import.
- [ ] Existing `tests/api/test_news.py` tests still pass.

## Testing
- pytest; command `pytest tests/api/test_news.py -q`.
- Follow existing fixtures in `tests/api/test_news.py` (FastAPI app with `router`, `dependency_overrides` for `get_current_user` / `get_db`, SQLite tmp repo, insert `NewsItemRow` via `Session`).
- Run the full API + repo tests touched: `pytest tests/api/test_news.py tests/unit -q -k news` and `ruff check src/`.

## Test scenarios
- Happy: 3 items with different `fetched_at` / `published_at` → max values returned.
- Edge: all `published_at` NULL → `newest_published_at` null, count still correct.
- Boundary: exactly 1 item.
- Error/auth: missing user override → 401/403 per existing pattern.
- Guard: static-source test for forbidden imports (AC6).
- Regression: `/news/unread-count` still resolves (route order not broken).

**Test plan:** ver `F178-test-plan.md` (fixtures: test-news-fastapi-app-dependency-overrides)

## Diagrams
Reflected in `F178-architecture.mmd` (owned by T06).
