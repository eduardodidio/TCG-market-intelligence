# F178-T01 — PRD, branch check and news XML fixtures

- **Wave:** 0
- **Type:** docs / setup
- **Depends on:** —
- **Status:** planned
- **Maps to AC:** AC9

## User Story
As a developer on the F178 Waves, I want the PRD and shared test fixtures in place before implementation, so that Wave 1 tasks can run unattended in parallel.

## Objective
Confirm the `homol` branch, write the PRD, and create the RSS/Atom/invalid XML fixtures that T02 (and T04) use.

## Dev Notes
Veja `_brief/00-overview.md`, `_brief/01-diagnosis.md` e `_brief/06-docs.md`.
- CLAUDE.md Gitflow: feature work on `homol`. Run `git branch --show-current`; if it is `main`, **stop and ask** (do not push, do not rebase). If it is a batch worktree branch created by the orchestrator, proceed.
- PRD template: `docs/prd/template.md`.
- Fixtures dir `tests/fixtures/news/` does not exist yet — create it.
- No `.claude/settings.json` change needed: later waves only run `pytest`, `npm test`, `ruff`, `python -m src.cli.main` — already the standard commands.
- No dependency install: `httpx` is already in `pyproject.toml`. Do **not** add `feedparser`.

## Implementation details
1. `docs/prd/F178-news-collection-bat.md` — problem, diagnosis table (copy from `_brief/01-diagnosis.md`), goals, non-goals, user stories, AC (from `00-overview.md`), a "Live validation log" section left empty for T08.
2. `tests/fixtures/news/rss_sample.xml` — RSS 2.0 with `xmlns:media="http://search.yahoo.com/mrss/"` and `xmlns:content="http://purl.org/rss/1.0/modules/content/"`, 4 items:
   - item A: title, link, HTML `description`, `pubDate` `Tue, 22 Sep 2026 14:30:00 +0000`, `<enclosure url=".../a.jpg" type="image/jpeg"/>` — title contains "banned".
   - item B: `media:content url=".../b.png"`, `pubDate` with `-0300` offset.
   - item C: no `description`, only `content:encoded`; `media:thumbnail`; unparseable `pubDate` `"sometime soon"`.
   - item D: missing `link` (must be skipped).
3. `tests/fixtures/news/atom_sample.xml` — Atom feed, 3 entries: one with `<link rel="alternate" href>` + `<summary>` + `<published>2026-09-20T10:00:00Z</published>`; one with `<link href>` (no rel) + `<content type="html">` + only `<updated>`; one with only `rel="self"` link besides alternate to check alternate is preferred.
4. `tests/fixtures/news/invalid.xml` — truncated / non-XML HTML body (e.g. `<html><body>403 Forbidden`).

## Acceptance criteria
- [ ] Branch confirmed (not `main`), recorded in the PRD header.
- [ ] PRD exists and cites the root causes.
- [ ] 3 fixture files exist; `python -c "import xml.etree.ElementTree as E; E.parse('tests/fixtures/news/rss_sample.xml'); E.parse('tests/fixtures/news/atom_sample.xml')"` succeeds and `invalid.xml` raises `ParseError`.

## Testing
- No code under test; validate each fixture parses: `python -c "import xml.etree.ElementTree as E; E.parse('tests/fixtures/news/rss_sample.xml'); E.parse('tests/fixtures/news/atom_sample.xml')"` (and `invalid.xml` must raise `ParseError`).
- Confirm the PRD exists: `test -f docs/prd/F178-news-collection-bat.md`.
- `pytest tests/ -q --co` must still collect (no code change expected).

## Test scenarios
- Happy: both valid fixtures parse with ElementTree.
- Edge: RSS fixture covers enclosure / media:content / media:thumbnail / content:encoded / missing link / bad date / non-UTC offset.
- Error: invalid fixture fails to parse.
- Boundary: Atom entry with both `self` and `alternate` links.

**Test plan:** ver `F178-test-plan.md` (fixtures: rss-sample-xml, atom-sample-xml, invalid-xml)

## Diagrams
None (owned by T06).
