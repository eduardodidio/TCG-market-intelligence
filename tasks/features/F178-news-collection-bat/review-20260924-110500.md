# F178 — Tech Lead Review

**Reviewer:** techlead agent
**Date:** 2026-09-24
**Branch:** `claude/stoic-mccarthy-nv2690` (batch worktree, not `homol`/`main` — confirmed by T01 and re-verified here; no push occurred)

Verified independently (not just re-reading Wave summaries):
- `python3 -m pytest tests/services/test_news_fetcher.py tests/api/test_news.py tests/cli/test_news_cmd.py -q` → **72 passed**. Global coverage-gate failure (13%) is expected/pre-existing — it reflects the whole `src/` tree, not this feature (`news_fetcher.py` itself: 92% line coverage).
- `ruff check src/services/news_fetcher.py src/api/routers/news.py src/database/repository.py src/cli/news_cmd.py src/cli/main.py` → clean.
- `cd frontend && npx vitest run src/pages/__tests__/NewsPage.test.tsx` → **16 passed** (only benign `act()` warnings from `markNewsUnread`'s fire-and-forget refetch, no failures).
- `grep -rn feedparser` across `src/`, `tests/`, `pyproject.toml` → zero hits outside a vendored `bs4` docstring; the AC1 root cause is genuinely fixed.
- `git diff --stat` across the feature commits → matches the files-touched table in the README exactly; no drive-by edits outside the plan (`src/api/app.py`, `models.py`, `App.tsx`, `Layout.tsx`, `pyproject.toml` untouched, as promised).

## T01 — PRD, branch check, fixtures

1. **Architecture:** N/A (docs/fixtures only).
2. **Code quality:** Fixtures (`rss_sample.xml`, `atom_sample.xml`, `invalid.xml`) are well-targeted — cover enclosure/media:content/media:thumbnail/content:encoded/missing-link/bad-date/offset-date and Atom's alternate-vs-fallback link and updated-vs-published precedence. `invalid.xml` correctly triggers `ET.ParseError`.
3. **Tests:** N/A — these are shared fixtures consumed by T02/T04 tests.
4. **Diagrams:** N/A (T06 owns these).
5. **Cross-task:** Branch check done correctly per Gitflow guardrail (stop only if `main`; batch worktree branch is fine to proceed on). PRD includes AC and an initially-empty "Live validation log" that T08 later filled — good forward design.

## T02 — `news_fetcher.py` rewrite (httpx + ElementTree)

1. **Architecture:** Clean separation — `parse_feed`/`fetch_feed` are pure/testable, `fetch_news` orchestrates with per-source try/except so one broken feed can't take down the run. `dry_run` short-circuits before any `repo.upsert_news_item` call and works with `repo=None`, exactly per contract.
2. **Code quality:** RSS `<enclosure>` handling correctly guards on `type` containing `"image"` or absent; `_atom_link` correctly prefers `rel=alternate`/no-rel over other rel values, falling back only if no alternate found — matches Atom spec intent. Date handling normalizes to naive UTC consistently via `_to_naive_utc`, matching the DB's naive `DateTime` columns (a documented dev-note requirement). `MAX_BODY_BYTES` guard against oversized payloads is a good defensive addition beyond spec. `except Exception` in the per-entry loop and per-source loop is broad but intentionally scoped (logged + counted as an error, doesn't abort the whole run) — appropriate for a fetcher that must survive malformed feed entries.
3. **Tests:** `test_news_fetcher.py` was fully rewritten to `httpx.MockTransport` instead of patching `feedparser`, as the task's "Test impact" section demanded. 92% line coverage on the module; uncovered lines are mostly narrow exception branches (128-129, 141-142) and the `owns_client` finally-close path — acceptable gaps, not core logic.
4. **Diagrams:** N/A (T06).
5. **Cross-task:** `dry_run`/`client` kwargs contract confirmed present exactly as T04 (Wave 2) and the brief require — no mismatch found.

## T03 — `GET /api/v1/news/status`

1. **Architecture:** `Repository.get_news_status()` added inside the existing `# ── News feed (F166)` block, right after `upsert_news_item`, as instructed — did not touch other repository sections. Router endpoint follows the same `Depends(get_current_user)` / `ApiResponse[...]` / `success_response(...)` pattern as sibling routes.
2. **Code quality:** Single aggregate query (`count`, `max(fetched_at)`, `max(published_at)`) — efficient, no N+1. Route ordering (`/status`, `/unread-count` both GET, no `GET /{news_id}`) means no path-shadowing risk despite being declared after the `POST /{news_id}/mark-read` routes.
3. **Tests:** `test_news.py` explicitly test-enforces AC6 (`TestNewsRouterNoNetworkImports.test_no_forbidden_imports` — asserts the router source has no `news_fetcher`/`httpx`/`feedparser`/`requests` import). Also a regression test (`test_unread_count_route_still_resolves`) guarding against `/status` shadowing `/unread-count` — good defensive test given the route-ordering concern above.
4. **Diagrams:** N/A.
5. **Cross-task:** Response shape (`total_items`, `last_fetched_at`, `newest_published_at`) matches `_brief/03-status-endpoint.md` and what T05's frontend consumes.

## T04 — `src/cli/news_cmd.py`

1. **Architecture:** Standalone module avoiding the circular import with `main.py`, as directed. `_resolve_db` is duplicated (5 lines) rather than imported — a deliberate, documented, small duplication to break the cycle; acceptable trade-off given the alternative (restructuring `main.py`) is out of scope.
2. **Code quality:** `--check-sources` correctly never constructs a `Repository` (verified by test asserting `Repository` is not called). Exit code 1 only when `stats["sources"]` is non-empty and *no* source succeeded — correctly matches AC3 ("exit 1 when every source fails; 0 otherwise"), including the edge case of an empty/filtered-to-nothing source list not spuriously failing.
3. **Tests:** `test_news_cmd.py`, 9 scenarios at this stage (10 after T07's registration test) — good coverage of `--check-sources`, `--source` filtering (including unknown-source `UsageError`), and exit codes.
4. **Diagrams:** N/A.
5. **Cross-task:** Depends on T02's `fetch_news(..., dry_run=, client=)` signature — confirmed compatible.

## T05 — NewsPage states

1. **Architecture:** Two independent `useApi` calls (list + status) composed correctly; `noData`/`filterEmpty`/`stale` derived as plain booleans from the two API results rather than duplicated state — clean.
2. **Code quality:** State matrix is exhaustive and mutually exclusive: no-data (status says 0 items) takes priority over filter-empty; "all read" (unread filter, has items overall) vs generic "filter empty" (other filter/category) are correctly distinguished and each gets its own recovery action (`Show all` vs `Clear filters`). Stale computed purely from `last_fetched_at` age (>72h), independent of item content, matching AC7. i18n edits are scoped to the `news` block only in both locale files, per the dev note about other batch features touching other blocks.
3. **Tests:** `NewsPage.test.tsx` mocks `fetchNewsStatus` and replaces the old "No news yet" test with a no-data-state test, exactly matching the documented "Test impact" changes. 16/16 pass; the `act()` warnings are pre-existing style (fire-and-forget `refetch()` after `markNewsRead`/`markNewsUnread`, not a new regression) and don't fail the suite — MINOR, not blocking.
4. **Diagrams:** N/A (T06 owns diagrams, but the journey diagram's reader lane accurately reflects this component's actual branching logic — cross-checked against the code, they match).
5. **Cross-task:** Consumes the exact `NewsStatus` shape T03 emits.

## T06 — Diagrams

1. **Architecture diagram:** Accurately shows the local/Render split — `.bat → CLI → fetcher → Neon` on one side, `Render (read-only) API → Repository → Neon` on the other, with the UI only ever talking to the API. This is the central architectural claim of the whole feature (no server-side network I/O) and the diagram states it correctly.
2. **Journey diagram:** Owner lane and reader lane both verified against the actual code paths (CLI exit-code branch, NewsPage state branches) — no drift between diagram and implementation.
3. Both diagrams exist at the paths specified in the task table. No issues.

## T07 — Shared files (main.py, .bat, README, ADR)

1. **Architecture:** `main.py` diff is minimal and additive as required — one import + one `add_command` line placed right before `if __name__ == "__main__":`, old inline `fetch-news` block fully removed (`grep feedparser` confirms nothing left). No dual registration (`cli.commands` test in T04/T07 asserts `fetch-news` appears exactly once).
2. **Code quality:** `bats/fetch-news.bat` mirrors `process-queue.bat`'s header/structure and correctly checks `errorlevel` after the CLI call (a small improvement over `process-queue.bat`, which doesn't check errorlevel at all — reasonable, since AC3's exit-code contract only exists for `fetch-news`). ADR-0019 is well-argued, uses the reserved number correctly, and honestly documents the alternative (APScheduler on Render) it rejected and why.
3. **Tests:** `TestRegisteredInMainCli` (added to `test_news_cmd.py`) asserts `fetch-news --help` exits 0 and the command is registered exactly once — a good regression guard against future re-introduction of the inline command.
4. **Diagrams:** N/A (covered by T06).
5. **Cross-task:** README section (`### F178 -- News Collection via Offline Routine`, 2026-09-24) exists and documents the fetcher rewrite, CLI flags, `.bat`, `/news/status`, `NEWS_FEED_SOURCES`, and NewsPage states — satisfies the CLAUDE.md "every shipped feature updates README" rule.

## T08 — Live source validation (planned, not done)

Correctly **not** force-completed. The cloud sandbox's outbound proxy returns a `CONNECT` 403 to all 5 default feed hosts; the PRD's validation log documents this plainly (with a raw `curl` cross-check proving it's the sandbox tunnel, not the feeds), and the task's own Dev Notes required stopping here rather than guessing at replacement URLs. `SOURCES` was correctly left untouched. This is a genuine external blocker, not a shortcut — appropriately escalated to the user with concrete next steps (run `--check-sources` locally, then `bats\fetch-news.bat` against Neon with explicit confirmation, per the Guardrails de Segurança on writes to shared production data).

## Global acceptance criteria — status

| AC | Status |
|---|---|
| 1. `fetch-news` works without `feedparser`, idempotent | Done — code + tests verified |
| 2. `--check-sources` per-source table, writes nothing | Done — test-enforced (`Repository` not called) |
| 3. Exit 1 iff every source fails | Done — verified in code and tests |
| 4. `bats/fetch-news.bat` exists, follows `process-queue.bat` pattern | Done |
| 5. `GET /api/v1/news/status` shape | Done |
| 6. News router imports no network/fetcher module | Done — test-enforced |
| 7. NewsPage state matrix + stale hint | Done — verified in code and passing tests |
| 8. pytest/npm test/ruff/build green | pytest, ruff, vitest independently re-run and green; frontend `npm run build` not re-run by this review (no reason to expect breakage — no new deps, TS types added correctly) |
| 9. PRD, ADR, both diagrams, README updated | Done |
| 10. Default sources validated live | **Blocked on user** — sandbox can't reach the internet; this is expected and correctly flagged, not a defect in the delivered code |

## Retrospective Seeds
- **Pattern:** When a Wave's task requires live external network validation (T08-style "run against the real internet"), the sandboxed developer agent cannot complete it — this should be flagged as a Wave-4-style "user-only" task at planning time (Architect), not discovered mid-Wave, so the Waves plan can schedule it last and mark it non-blocking for the rest of the pipeline (as F178 did, correctly, by accident of task ordering).
- **Role(s) affected:** architect
- **Lesson:** Any task whose Dev Notes anticipate "sandbox may block this, stop and ask" should be explicitly labeled a user-handoff task in the Waves table, so TechLead/QA don't wait on it before approving the rest of the feature.

## Verdict

**APPROVED_WITH_FOLLOWUP**

All code, tests, docs, and diagrams for T01–T07 are complete, correct, and independently verified (tests re-run, not just trusted from summaries). The only open item is T08's live source validation, which is genuinely blocked by the sandbox's outbound network restriction and requires the user to run `python -m src.cli.main fetch-news --check-sources` locally and confirm `bats\fetch-news.bat` against Neon — this is expected, was correctly escalated rather than faked, and does not reflect a defect in the shipped code. Follow-up: user runs T08's remaining steps; no further Developer/TechLead work needed unless `--check-sources` reveals a source that needs pruning from `SOURCES`.

DIDIO_DONE: techlead reviewed F178 verdict=APPROVED_WITH_FOLLOWUP
