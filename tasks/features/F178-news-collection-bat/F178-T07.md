# F178-T07 — Shared files: register CLI, add .bat, README, ADR

- **Wave:** 3
- **Type:** infra / docs
- **Depends on:** F178-T02, F178-T03, F178-T04, F178-T05, F178-T06
- **Status:** planned
- **Maps to AC:** AC4, AC8, AC9

## User Story
As the site owner, I want to double-click `bats/fetch-news.bat` to collect news into Neon, so that the News page on Render has content without server-side scraping.

## Objective
Do all edits to batch-shared files (`src/cli/main.py`, `bats/`, `README.md`) in one small task, plus the ADR.

## Dev Notes
Veja `_brief/00-overview.md`, `_brief/04-cli-bat.md` e `_brief/06-docs.md`.
- **Conflict hygiene:** before editing, `git pull`/rebase-free sync per orchestrator instructions; other batch features (F171–F179) may have touched `main.py`, `README.md`, `bats/` meanwhile. Keep diffs minimal: delete the inline `fetch-news` block, add 2 lines before `if __name__ == "__main__":`. Append (never reorder) if other `add_command` lines exist.
- ADR number: **0019** (reserved in `tasks/features/EXECUTION-PLAN-F171-F179.md`; do NOT use "next free"). File: `docs/adr/0019-news-collection-offline-bat.md`.
- `.bat`: copy structure of `bats/process-queue.bat` (LF endings as in repo).
- No CI/CD changes, no Render config changes.

## Implementation details
1. `src/cli/main.py`: remove `@cli.command("fetch-news") ... fetch_news_cmd` (~lines 2792-2818); add
   ```python
   from src.cli.news_cmd import fetch_news_command  # noqa: E402  (F178)

   cli.add_command(fetch_news_command)
   ```
2. `bats/fetch-news.bat` — content from `_brief/04-cli-bat.md`.
3. `README.md` — short F178 note: new routine, `--check-sources`, `/api/v1/news/status`, `NEWS_FEED_SOURCES`, new NewsPage states.
4. `docs/adr/<NNNN>-news-collection-offline-bat.md` — context/decision/alternatives/consequences.

## Acceptance criteria
- [ ] `python -m src.cli.main fetch-news --help` shows `--check-sources` and `--source`.
- [ ] `python -m src.cli.main --help` lists `fetch-news` exactly once.
- [ ] `bats/fetch-news.bat` exists; calls `python -m src.cli.main fetch-news`.
- [ ] README and ADR updated.
- [ ] Full suite green: `pytest tests/ --cov=src --cov-report=term-missing`, `ruff check src/`, `cd frontend && npm test && npm run build`.

## Testing
- Add to `tests/cli/test_news_cmd.py` (created by T04) a test `test_registered_in_main_cli`: `CliRunner().invoke(cli, ["fetch-news", "--help"])` exits 0 and contains `--check-sources`; and `list(cli.commands).count("fetch-news") == 1`.
- `.bat`: a test reading `bats/fetch-news.bat` asserting it contains `cd /d "%~dp0\.."` and `python -m src.cli.main fetch-news` (Windows not available in CI).
- Run: `pytest tests/cli/test_news_cmd.py -v`, `ruff check src/`, and `pytest tests/ -q -k "news or cli"`.

## Test scenarios
- Happy: main CLI resolves `fetch-news` to the new module.
- Regression: other CLI commands still import (`python -m src.cli.main --help` exits 0).
- Edge: no circular import (`python -c "import src.cli.main"`).
- Error: `.bat` prints `[FAIL]` branch when exit code ≠ 0 (static content check).

**Test plan:** ver `F178-test-plan.md` (fixtures: clirunner-patched-fetch-news, static-bat-content-check)

## Diagrams
None (T06). Verify diagram labels match the final file names.
