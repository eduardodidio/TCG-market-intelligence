# F58-T01 — Fix Gaucho i18n Typo + Extend Dismiss Duration

**Status:** done
**Wave:** 0
**Estimate:** tiny

## User Story

As a user with PILA currency, I want the gaucho dialog to display the
correct phrase and stay visible long enough to read the reply.

## Dev Notes

### i18n Fix

**File:** `frontend/src/i18n/locales/pt-BR.json` (line 549)

Current:
```json
"message": "Tchê, que tá o luxo do gaúcho esta coleção?"
```

Change to:
```json
"message": "Tchê, tá o luxo do gaúcho esta coleção?"
```

**File:** `frontend/src/i18n/locales/en.json` (line 549)

Check if EN has a corresponding translation that also needs the fix.
EN likely has a different phrasing — only fix if it contains "que" erroneously.

### Extend Auto-Dismiss Duration

**File:** `frontend/src/components/GauchoDialog.tsx` (line 34)

Current: `setTimeout(onDismiss, 2500)` — reply disappears after 2.5s

Change to: `setTimeout(onDismiss, 4500)` — reply stays for 4.5s (+2s)

The no-options auto-dismiss (line 18, `autoDismissMs` default 4000ms)
should remain unchanged — only the post-reply dismiss needs extending.

### Files to Modify

1. `frontend/src/i18n/locales/pt-BR.json` — fix "que tá" → "tá"
2. `frontend/src/components/GauchoDialog.tsx` — 2500 → 4500

## Testing

- Snapshot/unit test: verify pt-BR collection message text
- Unit test: GauchoDialog reply dismiss timer is 4500ms
- Manual: open app with PILA currency, navigate to /collection, verify dialog
