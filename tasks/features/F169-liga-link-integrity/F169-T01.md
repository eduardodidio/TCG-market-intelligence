# F169-T01 — CardDetail endpoint: use stored Liga URL

**Status:** planned

## User Story

As a user viewing the card detail page, I want the Liga link to point to the
exact page my price was scraped from, not a generic name search.

## Dev Notes

### File: `src/api/routers/cards.py`

**Current state (line 194):**
```python
ligamagic_url=liga_url_for_card_name(card.name_en) if card.name_en else None,
```

**Target state:**
Replace with a call to `resolve_liga_card_url()` from `src/providers/liga/urls.py`.

**Changes required:**

1. **Add import** (around line 37, replace existing import):
   - Remove: `from src.providers.liga.url import liga_url_for_card_name`
   - Add: `from src.providers.liga.urls import resolve_liga_card_url`

2. **Determine `is_foil`**: The `get_card` function (line 158) does not
   currently know if the card is foil. Since this is the generic card detail
   (not a collection entry), use `is_foil=False` as the default. The
   `resolve_liga_card_url` function already handles this correctly by only
   checking the non-foil key when `is_foil=False`.

3. **Replace line 194** with:
   ```python
   ligamagic_url=resolve_liga_card_url(
       card_id=card.id,
       is_foil=False,
       fallback_name=card.name_en or "",
       lookup=repo.get_liga_card_url,
   ),
   ```

4. **Verify** that `liga_url_for_card_name` is no longer imported in this file.
   If other code in the same file uses it, keep the import. Check with grep.

### Key functions involved

- `resolve_liga_card_url()` at `src/providers/liga/urls.py:48` — takes
  `card_id`, `is_foil`, `fallback_name`, `lookup` callable
- `repo.get_liga_card_url()` at `src/database/repository.py:4925` — takes
  `external_id: str`, returns `str | None`
- `liga_external_id()` at `src/providers/liga/urls.py:43` — builds
  `"liga_{card_id}"` or `"liga_{card_id}_foil"` (called internally by resolve)

## Testing

### New tests in `tests/api/test_cards_liga_url.py`

1. **test_card_detail_uses_stored_liga_url**: Mock `repo.get_liga_card_url` to
   return a valid Liga URL. Assert `ligamagic_url` in response matches the
   stored URL (not the name-based fallback).

2. **test_card_detail_falls_back_to_name_url**: Mock `repo.get_liga_card_url`
   to return `None`. Assert `ligamagic_url` is a name-based search URL
   containing the card name.

3. **test_card_detail_liga_url_none_when_no_name**: Card with `name_en=None`
   and no stored URL. Assert `ligamagic_url` is `None`.

4. **test_card_detail_rejects_invalid_stored_url**: Mock `repo.get_liga_card_url`
   to return an invalid URL (e.g., `"http://evil.com"`). Assert the endpoint
   falls back to the name-based URL (validation inside `resolve_liga_card_url`).

### Existing tests

- Run `pytest tests/api/ -k card` to verify no regressions.
