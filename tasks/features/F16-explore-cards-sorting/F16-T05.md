# Task F16-T05 — Tests and documentation

**Feature:** F16
**Wave:** 3
**Type:** test + docs
**Depends on:** F16-T01, F16-T02, F16-T03, F16-T04
**Status:** planned

## User Story

As a maintainer, I want comprehensive test coverage and updated docs for
the sorting feature, so it remains reliable as the project evolves.

## Objective

Ensure backend and frontend tests cover all sort fields and edge cases.
Update README.md with the new feature. Verify coverage thresholds.

## Dev Notes

- Relevant files:
  - `tests/` — backend test directory
  - `frontend/src/` — frontend test files (co-located or in `__tests__/`)
  - `README.md` — project README

- Backend tests to verify:
  - Repository: each sort_by + sort_dir combination
  - API: query param validation (422 on invalid sort_by)
  - API: offset pagination with sort
  - Backward compat: cursor param still works

- Frontend tests to verify:
  - SortSelect component renders, fires events
  - MyCollection page with sort params in URL
  - Client-side price sorting logic
  - Offset-based load-more

- Run full test suite and check coverage:
  - Backend: `python -m pytest --cov=src --cov-report=term-missing`
    (must stay >= 70%, currently at 92.75%)
  - Frontend: `cd frontend && npx vitest run`
    (currently 304 tests)

- Update `README.md`:
  - Add note under collection features about sorting support
  - Mention available sort fields

## Testing

- Framework: pytest (backend), Vitest (frontend)
- Commands:
  - `python -m pytest -v`
  - `cd frontend && npx vitest run --reporter=verbose`

## Implementation details

- Add/update test files as needed based on gaps found after T01-T04
- File: `README.md` — add F16 to feature list
- Verify no regressions in existing test suite

## Acceptance criteria

- [ ] Backend test coverage stays >= 70% (target: maintain ~92%)
- [ ] All new backend code has unit tests
- [ ] Frontend SortSelect has unit tests
- [ ] Frontend MyCollection sort integration has tests
- [ ] Full backend test suite passes
- [ ] Full frontend test suite passes
- [ ] README.md updated with sorting feature description
- [ ] Pre-commit hooks (ruff) pass

## Test scenarios

- **Integration:** end-to-end: API returns cards sorted by name, verify
  order in response JSON
- **Regression:** existing collection tests still pass unchanged
- **Edge case:** empty collection with sort params returns empty list
