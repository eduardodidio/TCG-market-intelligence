# F73-T02 — Backend Tests

**Status:** planned
**Wave:** 0
**Depends on:** F73-T01

## User Story

As a developer, I want tests for the stale scan detection logic so regressions are caught.

## Dev Notes

### Files to create/modify

1. **`tests/unit/test_scan_hygiene.py`** (NEW)
   - Test `mark_stale_scans_as_error`:
     - Creates scan_runs with status="running" and started_at yesterday → should be marked error
     - Creates scan_runs with status="running" and started_at today → should NOT be marked
     - Creates scan_runs with status="completed" and started_at yesterday → should NOT be marked
     - Verifies error_summary and finished_at are set
   - Test `delete_all_scan_runs`:
     - Creates multiple scan_runs → deletes all → count is 0

2. **`tests/unit/test_cli.py`** (MODIFY if CLI command added)
   - Test `db-reset-scans` requires --confirm
   - Test `db-reset-scans --confirm` calls delete_all_scan_runs

## Testing

- All new tests pass
- Existing scan tests still pass
