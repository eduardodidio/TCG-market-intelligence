# F12-T05 -- API Schema: SnapshotRequest

- **Wave:** 2
- **Type:** backend
- **Depends on:** (none)
- **Status:** done

## User Story

As a developer, I need a Pydantic schema for the snapshot-prices API
request body, so that the API endpoint can validate input parameters.

## Objective

Add `SnapshotRequest` Pydantic model to `src/api/schemas/collection.py`.

## Dev Notes

- Simple Pydantic model with optional `limit` (int | None) field.
- Follow the pattern of the existing `SyncRequest` model in the same file.
- No dependency on any other F12 task -- just a data schema.

## Implementation Details

1. Open `src/api/schemas/collection.py`.
2. Add after `SyncRequest`:

```python
class SnapshotRequest(BaseModel):
    limit: int | None = None
```

## Acceptance Criteria

- [x] `SnapshotRequest` exists in `src/api/schemas/collection.py`
- [x] Has `limit: int | None = None` field
- [x] Importable from `src.api.schemas.collection`
- [x] Existing schema tests still pass

## Testing

- **Framework:** pytest
- **Command:** `python -m pytest tests/api/ -x -q`
- **Scenarios:**
  1. `SnapshotRequest()` instantiates with defaults (limit=None)
  2. `SnapshotRequest(limit=10)` sets limit correctly
  3. JSON serialization round-trips correctly

## Diagrams

- Contributes to: `docs/diagrams/F12-architecture.mmd`
