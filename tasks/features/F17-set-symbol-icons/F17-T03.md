# F17-T03: Wire SetIconFilter into MyCollection

- **Wave:** 2
- **Status:** done
- **Depends on:** F17-T02
- **Description:**
  Update `frontend/src/pages/MyCollection.tsx` to use the new
  `SetIconFilter` component instead of `FilterChips` for the set filter.

  Changes:
  1. Replace `import { FilterChips }` with
     `import { SetIconFilter }` from `../components/SetIconFilter`.
  2. Replace `<FilterChips options={setOptions} .../>` with
     `<SetIconFilter options={setOptions} .../>` -- same props, drop-in
     swap.
  3. The `setOptions` mapping (lines 172-177) can be simplified since the
     label no longer needs the `"SET -- Name"` format. Change to just
     `label: s.set_name || s.set_code` (the set code is shown as the
     icon, tooltip shows the name).
  4. Remove the now-unused `FilterChips` import (but do NOT delete
     `FilterChips.tsx` -- it is still used by `Cards.tsx`).

  Update `frontend/tests/pages/MyCollection.test.tsx` to account for the
  new component (update any assertions that reference `filter-chip-*`
  test IDs to use `set-icon-*` test IDs, or adjust as needed).

- **Acceptance Criteria:**
  - [ ] MyCollection page renders `SetIconFilter` instead of `FilterChips`
  - [ ] Set filtering still works end-to-end (select set, cards filter, deselect)
  - [ ] URL param sync (`?set=DMR`) still works
  - [ ] `FilterChips` component and its test file are NOT deleted
  - [ ] `Cards.tsx` still uses `FilterChips` (unaffected)
  - [ ] MyCollection test file updated and passing
  - [ ] All existing frontend tests pass (`npm run test`)

- **Files to touch:**
  - `frontend/src/pages/MyCollection.tsx` (modify)
  - `frontend/tests/pages/MyCollection.test.tsx` (modify)
