# F11-T08 — Documentation (PRD, Diagrams, README)

- **Wave:** 3
- **Type:** docs
- **Depends on:** F11-T06
- **Status:** done

## User Story

As a project maintainer, I want F11 documented with a PRD, diagrams, and
README update, so that the operational pipeline is recorded and the
project README reflects the current state.

## Objective

Create PRD, update/create Mermaid diagrams, and update the project README.

## Dev Notes

Veja `_brief/00-overview.md`.

- PRD path: `docs/prd/F11-post-f10-operationalization.md`
- Diagrams: `docs/diagrams/F11-architecture.mmd`, `docs/diagrams/F11-journey.mmd`
- README.md: add F11 section summarizing what was delivered
- Convention: diagrams are Mermaid `.mmd` files, living documentation

## Implementation Details

1. **PRD** — create `docs/prd/F11-post-f10-operationalization.md`:
   - Problem statement, goals, scope
   - Reference F10 as prerequisite
   - List operational steps and tech debt items

2. **Architecture diagram** — `docs/diagrams/F11-architecture.mmd`:
   ```mermaid
   flowchart TD
     subgraph "Wave 0"
       NORM[normalize_set_codes.py] --> DB[(tcg_market.db)]
     end
     subgraph "Wave 1 (parallel)"
       MR[match-report] --> MYP[MYP Search API]
       TD1[extract _row_to_entry]
       TD2[move SQLAlchemy to Repo]
       TD3[remove BASE_URL]
     end
     subgraph "Wave 2"
       SYNC[sync-collection] --> MYP
       SYNC --> DB
     end
     subgraph "Wave 3"
       DASH[verify dashboard]
       DOCS[documentation]
     end
     NORM --> MR
     NORM --> SYNC
     MR --> SYNC
   ```

3. **Journey diagram** — `docs/diagrams/F11-journey.mmd`:
   - Operator journey: backup → normalize → match-report → review →
     sync → verify dashboard → done

4. **README.md** — add section:
   - F11 shipped: DB normalized, collection synced, tech debt cleaned,
     dashboard KPIs verified

## Acceptance Criteria

- [ ] PRD exists at `docs/prd/F11-post-f10-operationalization.md`
- [ ] `docs/diagrams/F11-architecture.mmd` exists and renders
- [ ] `docs/diagrams/F11-journey.mmd` exists and renders
- [ ] `README.md` updated with F11 delivery notes

## Testing

- **Framework:** n/a (documentation)
- **Verification:** diagrams render correctly in Mermaid preview

## Test Scenarios

1. **Happy path:** all docs created, diagrams render, README updated
2. **Verification:** cross-links between PRD and task files are valid

## Diagrams

- Creates: `docs/diagrams/F11-architecture.mmd` (if not created by T01)
- Creates: `docs/diagrams/F11-journey.mmd` (if not created by T07)
