# F11-T05 — Remove Dead `BASE_URL` from sync_collection.py

- **Wave:** 1
- **Type:** backend
- **Depends on:** F11-T01
- **Status:** done

## User Story

As a developer, I want dead code removed from the sync module, so that
the codebase stays clean and doesn't mislead future readers.

## Objective

Delete the unused `BASE_URL` constant from `src/collectors/sync_collection.py`.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/02-tech-debt.md`.

- `src/collectors/sync_collection.py:19` — `BASE_URL = "https://mypcards.com"`
- Never referenced anywhere in the file or any import.
- The provider (`MypCardsProvider`) handles its own base URL internally.

## Implementation Details

1. Delete line 19 from `src/collectors/sync_collection.py`:
   ```python
   BASE_URL = "https://mypcards.com"
   ```
2. Remove the blank line after it (line 20) to keep spacing clean.
3. Run `python -m pytest tests/ -x -q` to confirm no regressions.
4. Run `ruff check src/collectors/sync_collection.py` to confirm no lint issues.

## Acceptance Criteria

- [ ] `BASE_URL` no longer exists in `sync_collection.py`
- [ ] No references to `BASE_URL` anywhere in the codebase
- [ ] All tests pass
- [ ] Ruff lint clean

## Testing

- **Framework:** pytest
- **Command:** `python -m pytest tests/ -x -q`
- **Note:** trivial deletion — existing tests provide full coverage

## Test Scenarios

1. **Happy path:** delete line, all tests pass, lint clean
2. **Verification:** `grep -r "BASE_URL" src/collectors/` returns nothing

## Diagrams

None (trivial cleanup).
