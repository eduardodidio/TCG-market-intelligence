# F164-T02: Tests for fire-and-forget behavior

**Status:** completed
**Wave:** 0

## User Story

As a developer, I want comprehensive tests for the fire-and-forget refresh UX so regressions are caught.

## Dev Notes

### Files to modify
- `frontend/src/components/__tests__/CardTile.test.tsx` (update existing tests)

### Changes

1. **Update existing refresh tests** to match new behavior:
   - Remove any assertions about polling states
   - Assert button re-enables after enqueue (not after poll completion)
   - Assert no `usePriceRequestPolling` dependency

2. **New test cases**:
   - `shows enqueued feedback for 1.5s then resets`
   - `button is clickable again after enqueue feedback`
   - `does not poll for price request status`
   - `handles rapid double-click (debounce during refreshing)`
   - `error state clears after 3s`

## Testing

- All tests use `vitest` + `@testing-library/react`
- Mock `refreshCardPrice` API call
- Use `vi.advanceTimersByTime` for timeout assertions
