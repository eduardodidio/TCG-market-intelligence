# F42-T08: i18n + tests + diagrams

**Status:** planned
**Wave:** 4

## User Story

As the last task in the feature, ensure all strings are translated, test
coverage is adequate, and architecture diagrams are up to date.

## Dev Notes

### i18n keys

Add to `frontend/src/i18n/locales/en.json`:
```json
{
  "banEngine.alertBanned": "{{count}} card(s) in your collection are banned",
  "banEngine.alertRestricted": "{{count}} card(s) restricted",
  "banEngine.alertRecent": "{{count}} changed in the last 30 days",
  "banEngine.alertDismiss": "Dismiss",
  "banEngine.badgeBanned": "BANNED",
  "banEngine.badgeRestricted": "RESTRICTED",
  "banEngine.legalityTitle": "Format Legality",
  "banEngine.legalityUnavailable": "Legality data not available for unlinked cards",
  "banEngine.legalityEmpty": "No legality data synced yet",
  "banEngine.showAll": "Show all {{count}} formats",
  "banEngine.showLess": "Show less",
  "banEngine.newChip": "NEW",
  "banEngine.changedFrom": "Changed from {{oldStatus}} on {{date}}",
  "banEngine.bannedInFormats": "Banned in {{count}} format(s)",
  "banEngine.restrictedInFormats": "Restricted in {{count}} format(s)",
  "banEngine.allLegal": "Legal in all formats",
  "banEngine.recentChange": "Recent ban change"
}
```

Add corresponding PT-BR translations to `locales/pt-BR.json`:
```json
{
  "banEngine.alertBanned": "{{count}} carta(s) na sua colecao estao banidas",
  "banEngine.alertRestricted": "{{count}} carta(s) restritas",
  "banEngine.alertRecent": "{{count}} alterada(s) nos ultimos 30 dias",
  "banEngine.alertDismiss": "Fechar",
  "banEngine.badgeBanned": "BANIDA",
  "banEngine.badgeRestricted": "RESTRITA",
  "banEngine.legalityTitle": "Legalidade por Formato",
  "banEngine.legalityUnavailable": "Dados de legalidade indisponiveis para cartas nao vinculadas",
  "banEngine.legalityEmpty": "Nenhum dado de legalidade sincronizado",
  "banEngine.showAll": "Mostrar todos os {{count}} formatos",
  "banEngine.showLess": "Mostrar menos",
  "banEngine.newChip": "NOVO",
  "banEngine.changedFrom": "Alterado de {{oldStatus}} em {{date}}",
  "banEngine.bannedInFormats": "Banida em {{count}} formato(s)",
  "banEngine.restrictedInFormats": "Restrita em {{count}} formato(s)",
  "banEngine.allLegal": "Legal em todos os formatos",
  "banEngine.recentChange": "Alteracao recente de ban"
}
```

### Test coverage review

Backend (target: maintain 94%+ coverage):
- `tests/unit/services/test_ban_analyzer.py` — all 3 functions
- `tests/unit/database/test_repository_bans.py` — all 4 query methods
- `tests/integration/api/test_collection_banned.py` — endpoint tests

Frontend (target: maintain existing ratio):
- `tests/components/BanAlertBanner.test.tsx`
- `tests/components/BanBadge.test.tsx`
- `tests/components/LegalityPanel.test.tsx`
- `tests/pages/MyCollection.ban.test.tsx` — integration of banner + badges
- `tests/api/banEngine.test.ts` — API client tests

### Diagrams

Create or update under `docs/diagrams/`:

1. `F42-architecture.mmd` — data flow diagram showing:
   - user_collection -> card_legalities JOIN
   - BanAnalyzer service layer
   - API endpoint -> Frontend components

2. `F42-journey.mmd` — user journey:
   - User opens My Collection
   - Sees ban alert banner (if applicable)
   - Scrolls grid, spots red BANNED badges
   - Clicks card -> sees Format Legality panel
   - Sees "NEW" chips on recently changed statuses

### README update

Add F42 entry to project README.md under shipped features.

## Testing

- Verify all i18n keys render correctly in both languages
- Verify no missing translation warnings in console
- Run full backend test suite, confirm coverage >= 94%
- Run full frontend test suite, confirm no regressions
