# F176-T09 — `/cards/{id}/history` usa `build_history`

**Wave:** 3
**Type:** backend
**Depends on:** F176-T07
**Status:** planned
**Maps to AC:** AC9

## User Story
As a usuário navegando da coleção para a página do card, I want ver o mesmo histórico (variante normal) na página do card, so that as duas telas não se contradigam.

## Objective
Substituir a leitura de séries em `get_history` (`src/api/routers/cards.py` ~L208–262) por `build_history(repo, card_id, is_foil=False, days)`, com `meta` e `source`.

## Dev Notes
Veja `_brief/00-overview.md`, `_brief/02-key-resolution.md` e `_brief/04-api-ui.md`.

- Hoje: early-return se não houver source_cards; loop com `source=[sc.source, "jsonld_snapshot", "daily_snapshot"]` — ignora `liga_{card_id}` e `manual_{card_id}`.
- Editar só `get_history` + imports. **Não** tocar `get_price_trends` / `get_price_series_batch` (fora do escopo; F175 pode mexer em trends/market). Se o diagnóstico (T01) mostrou o mesmo defeito em trends, deixar registrado no ADR como follow-up (T02) — não corrigir aqui.
- Endpoint público (sem auth) — manter.
- 404 para card inexistente mantido.

## Implementation details
- `src/api/routers/cards.py`: `get_history`, imports (`build_history`, `PriceHistoryMeta`).
- `tests/api/test_cards_history_resolver.py` (novo); rodar `tests/api/test_cards_router.py` e `tests/integration/test_snapshot_pipeline.py` (usa `cards_router`).

## Acceptance criteria
- [ ] Card só-Liga (sem source_cards) retorna pontos (antes vazio).
- [ ] Série normal nunca inclui `liga_{id}_foil`.
- [ ] `meta.variant == "normal"`.
- [ ] Testes existentes de cards/snapshot pipeline verdes.

## Testing
- pytest + TestClient; fixtures de `tests/integration/test_snapshot_pipeline.py` (monta `FastAPI()` com `cards_router` e override de `get_db`).
- `pytest tests/api/test_cards_history_resolver.py tests/api/test_cards_router.py tests/integration/test_snapshot_pipeline.py -q`.
- Lint: `ruff check src/` limpo.

## Test scenarios
- Happy só-Liga → pontos.
- Com source_card MYP + liga mesmo dia → 1 ponto (liga).
- Foil presente e normal ausente → `observations=[]`, `meta.first_observed_at=None`.
- Manual → aparece com prioridade máxima.
- Card inexistente → 404; `period` inválido → 422.
- Card sem preço → `observations=[]`, `summary` coerente (sem exceção em `compute_price_change_summary` com lista vazia — verificar comportamento atual e manter).

## Diagrams
Nenhum.
