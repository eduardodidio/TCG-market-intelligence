# F176-T11 — Arquivos compartilhados: CLI `--dry-run`, `bats/daily-snapshot.bat`, README

**Wave:** 4
**Type:** infra / docs
**Depends on:** F176-T05, F176-T06, F176-T08, F176-T09, F176-T10
**Status:** done
**Maps to AC:** AC7, AC12, AC13

## User Story
As a operador local (Windows), I want uma rotina `.bat` diária e um backfill com pré-visualização, so that eu mantenha o histórico crescendo e possa preencher lacunas com segurança.

## Objective
Concentrar numa task pequena todas as edições em arquivos de alto risco do lote: flag `--dry-run` no `backfill-snapshots`, novo `bats/daily-snapshot.bat` e nota da F176 no `README.md`.

## Dev Notes
- **Governance:** `push-all.bat` is not in the repo. The automatic snapshot at the end of `liga-sweep` (T06) is the primary mechanism, and `bats/daily-snapshot.bat` is a manual/backfill fallback. The README must say this explicitly.
Veja `_brief/00-overview.md` e `_brief/03-snapshot-backfill.md`.

- **Alto risco de conflito** (lote F171–F179): `src/cli/main.py`, `README.md`, `bats/`. Editar o mínimo possível e nunca reformatar. Sem `git rebase` em branch compartilhada (CLAUDE.md); a sincronização da branch é responsabilidade do orquestrador.
- `src/cli/main.py` L3143–3166 `backfill_snapshots_cmd(db, days)`: adicionar `@click.option("--dry-run", is_flag=True, help="Only count what would be inserted")` e passar `dry_run=dry_run` para `backfill_snapshots` (T05). Mensagem: `"Backfill dry-run: {count} observations would be created."` quando dry-run. Não mexer em nenhum outro comando.
- `bats/daily-snapshot.bat` (novo) — copiar formato de `bats/process-queue.bat`:
  ```bat
  @echo off
  REM ============================================================
  REM  daily-snapshot.bat — Grava 1 ponto diario de historico por card
  REM  (carry-forward ate 30 dias apos a ultima coleta real). Idempotente.
  REM  Agendar: Windows Task Scheduler 1x/dia (ex.: 23:30)
  REM ============================================================
  cd /d "%~dp0\.."
  echo  TEDHC Daily Snapshot - %date% %time%
  python -m src.cli.main daily-snapshot
  echo  [DONE] %date% %time%
  ```
  CRLF line endings não são obrigatórios; seguir o arquivo existente (`file bats/process-queue.bat`).
- `README.md`: (1) seção `## Commands` — linhas para `daily-snapshot`, `backfill-snapshots --days N [--dry-run]` e `bats/daily-snapshot.bat`; (2) nova subseção no fim de `## Shipped`: `### F176 -- Collection Price History (definitive fix) (YYYY-MM-DD)` com: causa raiz em 1 frase, link ADR, endpoints alterados (`/collection/{id}/history`, `/collection/{id}/metrics`, `/cards/{id}/history` com `meta` + `source`), comportamento do snapshot pós-sweep, backfill forward-fill, `.bat`, UI. Adicionar ao **fim** da seção para reduzir conflitos.
- Recomendação operacional para o README: após o deploy, rodar uma vez `python -m src.cli.main backfill-snapshots --days 90 --dry-run` e depois sem `--dry-run` (grava no Neon via `.env`) — **o dev não executa contra o Neon**.

## Implementation details
- `src/cli/main.py` (só `backfill_snapshots_cmd`), `bats/daily-snapshot.bat` (novo), `README.md`, `tests/test_cli_snapshot.py` (casos novos).

## Acceptance criteria
- [ ] `python -m src.cli.main backfill-snapshots --help` mostra `--dry-run`.
- [ ] `--dry-run` não escreve (teste com CliRunner + SQLite tmp).
- [ ] `.bat` segue o padrão de `process-queue.bat` e chama `daily-snapshot`.
- [ ] README com comandos e bloco F176 em `## Shipped`.
- [ ] Diff de `main.py` restrito à função `backfill_snapshots_cmd`.
- [ ] `pytest tests/test_cli_snapshot.py -q` verde; `ruff check src/` limpo.

## Testing
- pytest + `click.testing.CliRunner` (padrão de `tests/test_cli_snapshot.py`);
- `--db sqlite:///<tmp_path>/t.db`.
- `bats/` está no `.gitignore`: versionar o novo arquivo com `git add -f bats/daily-snapshot.bat`.

## Test scenarios
- Happy: `backfill-snapshots --days 5 --db ...` com 1 observação real de D-3 → cria 3 (D-2..D) e imprime contagem.
- `--dry-run` → mesma contagem, 0 linhas novas no banco.
- `--days 0` → exit code ≠ 0 com mensagem de erro amigável (capturar `ValueError` → `click.BadParameter`).
- `daily-snapshot` inalterado (teste existente continua verde).
- `.bat`: teste simples em pytest que lê o arquivo e verifica `python -m src.cli.main daily-snapshot` e `cd /d "%~dp0\.."`.

**Test plan:** ver `F176-test-plan.md` (fixtures: `repo`)

## Diagrams
Nenhum.
