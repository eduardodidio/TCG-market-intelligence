# F175-T01 — Branch + PRD

- **Wave:** 0
- **Type:** docs / infra
- **Depends on:** —
- **Status:** planned

## User Story
As a product owner, I want a PRD that records the root cause and the fix scope for the empty market trending list, so that the implementation and review have a shared, written contract.

## Objective
Confirm the work is on branch `homol`, then write `docs/prd/F175-trending-market-mode.md`.

## Dev Notes
Veja `_brief/00-overview.md`, `_brief/01-root-cause.md` e `_brief/05-docs.md`.
- CLAUDE.md Gitflow: feature work happens on `homol`. Run `git branch --show-current`. If it is not
  `homol` (or a worktree branch the orchestrator created for this batch), stop and report it; do NOT
  switch branches with uncommitted work, and do NOT push.
- PRD template: `docs/prd/template.md`. Write it in Portuguese, like the other PRDs.

## Implementation details
- Create `docs/prd/F175-trending-market-mode.md` with: Problem, Root cause (the 2 causes), Goals,
  Non-goals, Solution summary (T03/T04/T05), Acceptance criteria (AC1–AC7), Risks (query volume on
  Neon, foil/non-foil price mixing).

## Acceptance criteria
- [ ] Branch confirmed and recorded in the task's completion note.
- [ ] PRD exists, follows the template, lists AC1–AC7.

## Testing
Docs only; no automated tests. Check that the file renders (valid Markdown).

## Test scenarios
- Happy: PRD has every section from the template.
- Edge: the branch is not `homol` → report and stop (no destructive git action).

## Diagrams
None (T06 owns them).
