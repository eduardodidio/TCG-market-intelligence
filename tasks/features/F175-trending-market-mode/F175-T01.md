# F175-T01 — Branch + PRD

- **Wave:** 0
- **Type:** docs / infra
- **Depends on:** —
- **Status:** done
- **Maps to AC:** AC7

## User Story
As a product owner, I want a PRD that records the root cause and the fix scope for the empty market trending list, so that the implementation and review have a shared, written contract.

## Objective
Confirm the work is on branch `homol`, then write `docs/prd/F175-trending-market-mode.md`.

## Dev Notes
Veja `_brief/00-overview.md`, `_brief/01-root-cause.md` e `_brief/05-docs.md`.
- CLAUDE.md Gitflow: feature work happens on `homol`. Run `git branch --show-current`. If it is not
  `homol` (or a worktree branch the orchestrator created for this batch — for this run the orchestrator branch is `claude/stoic-mccarthy-nv2690`, which is VALID), stop and report it; do NOT
  switch branches with uncommitted work, and do NOT push.
- PRD template: `docs/prd/template.md`. Write it in Portuguese, like the other PRDs.

## Implementation details
- Create `docs/prd/F175-trending-market-mode.md` with: Problem, Root cause (the 2 causes), Goals,
  Non-goals, Solution summary (T03/T04/T05), Acceptance criteria (AC1–AC7), Risks (query volume on
  Neon, foil/non-foil price mixing).

## Acceptance criteria
- [ ] Branch confirmed and recorded in the task's completion note.
- [ ] PRD exists, follows the template, lists AC1–AC7.

## Testing
- Docs only; no automated test file is created.
- Check: `test -f docs/prd/F175-trending-market-mode.md` and that it renders as valid Markdown (headings, lists, tables).
- Check: `git branch --show-current` is not `main`, and `ruff check src/` + `pytest tests/ -q` still pass (no code change).

## Test scenarios
- Happy: PRD has every section from the template.
- Edge: the branch is not `homol` → report and stop (no destructive git action).

**Test plan:** ver `F175-test-plan.md` (fixtures: none)

## Diagrams
None (T06 owns them).

## Notes from Developer
- Branch confirmed: `git branch --show-current` → `claude/stoic-mccarthy-nv2690`, the orchestrator
  worktree branch explicitly marked VALID for this run in the Dev Notes above. Not `homol`/`main`;
  no branch switch performed, no push performed.
- `docs/prd/F175-trending-market-mode.md` created following the style of the other in-batch PRD
  (`F176-collection-price-history.md`) rather than the generic `template.md` skeleton, since that
  is the convention already established for this lote (F171–F179). Contains Problema, Causa raiz
  (causa 1 principal + causa 2 agravante), Objetivos, Não-objetivos, Solução (T03/T04/T05),
  Critérios de aceite AC1–AC7, and Riscos (volume de consulta no Neon, mistura de preço
  foil/não-foil).
- `ruff check src/` → clean. `pytest tests/ -q` (via `python -m pytest`, since the venv on PATH
  points to a separate uv-managed pytest without `pytest-cov`) has one pre-existing collection
  error unrelated to this change: `tests/services/test_news_fetcher.py` fails to import because
  `feedparser` is not installed in this environment. No code was touched by this task.
