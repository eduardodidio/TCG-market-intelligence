# F88-T01: Gitflow Setup (homol/main)

**Status:** planned
**Wave:** 0
**Depends on:** —

## User Story

As a developer, I want a `homol` branch for development/testing and `main` for production deploys, so that I can validate changes locally before promoting to Render.

## Dev Notes

1. Create `homol` branch from current `main`
2. Update CLAUDE.md with gitflow workflow:
   - `homol` = development/homologation branch (all work happens here)
   - `main` = production (Render auto-deploys from main)
   - Promotion: user validates on homol, then merges to main
3. Add guardrail: never push directly to main without user confirmation
4. Update `.claude/settings.json` if needed to reflect default branch

### Files to create/modify
- `CLAUDE.md` — add Gitflow section

### Acceptance Criteria
- [ ] `homol` branch exists
- [ ] CLAUDE.md documents the homol→main workflow
- [ ] Guardrails section updated with branch protection rules

## Testing

- Manual: verify `git branch` shows `homol`
- Manual: verify CLAUDE.md has gitflow section
