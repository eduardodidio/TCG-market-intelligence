# F35-T06: Navigation + i18n + integration

**Status:** planned
**Wave:** 3

## User Story

As a user, I want the Top Decks page accessible from the app navigation
and all new UI text translated to English and Portuguese.

## Dev Notes

### Modified file: `frontend/src/App.tsx`

Add route: `<Route path="/decks/ranking" element={<TopDecksPage />} />`

Place it BEFORE the `/decks/:id` route to avoid route conflict (same
pattern as backend where `/ranking` must precede `/{deck_id}`).

Import `TopDecksPage` from `../pages/TopDecksPage`.

### Navigation update

Add "Top Decks" link to the sidebar/nav component (identify the correct
file — likely a Layout or Sidebar component). Place it near the existing
"Decks" link. Icon suggestion: trophy or chart-bar.

### Modified files: i18n locales

#### `frontend/src/i18n/locales/en.json`

Add keys under a `topDecks` namespace:
```json
{
  "topDecks": {
    "title": "Top Decks by Value",
    "sortByValue": "Sort by Value",
    "sortByChange": "Sort by Change %",
    "sortByCards": "Sort by Card Count",
    "period7d": "7 days",
    "period30d": "30 days",
    "period90d": "90 days",
    "totalValue": "Total Value",
    "valueChange": "Value Change",
    "pricedCards": "{{priced}} of {{total}} priced",
    "noDecks": "No decks to rank yet",
    "noDecksHint": "Import a deck to see it ranked here",
    "loadMore": "Load More",
    "showHistory": "Show value history",
    "hideHistory": "Hide value history",
    "noPriceData": "No price data available",
    "deckValue": "Deck Value"
  }
}
```

#### `frontend/src/i18n/locales/pt-BR.json`

Add corresponding PT-BR translations:
```json
{
  "topDecks": {
    "title": "Top Decks por Valor",
    "sortByValue": "Ordenar por Valor",
    "sortByChange": "Ordenar por Variacao %",
    "sortByCards": "Ordenar por Qtd. de Cartas",
    "period7d": "7 dias",
    "period30d": "30 dias",
    "period90d": "90 dias",
    "totalValue": "Valor Total",
    "valueChange": "Variacao de Valor",
    "pricedCards": "{{priced}} de {{total}} com preco",
    "noDecks": "Nenhum deck para ranquear",
    "noDecksHint": "Importe um deck para ve-lo no ranking",
    "loadMore": "Carregar Mais",
    "showHistory": "Mostrar historico de valor",
    "hideHistory": "Ocultar historico de valor",
    "noPriceData": "Sem dados de preco",
    "deckValue": "Valor do Deck"
  }
}
```

### Integration check

Verify all pages use `useTranslation()` for new text. Ensure
`TopDecksPage` is wrapped in `ProtectedRoute` (auth required).

### Files to modify
- `frontend/src/App.tsx` — add route
- Sidebar/nav component — add "Top Decks" link
- `frontend/src/i18n/locales/en.json` — add topDecks keys
- `frontend/src/i18n/locales/pt-BR.json` — add topDecks keys

## Testing

- Route `/decks/ranking` renders TopDecksPage
- Navigation link exists and points to `/decks/ranking`
- i18n keys resolve in both EN and PT-BR
- Page is behind auth (ProtectedRoute)
