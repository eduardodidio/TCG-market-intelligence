# F64-T01: Wrap logo text in Link component

**Status:** done
**Wave:** 0
**Parallel with:** F63-T01, F68-T01, F70-T01

## User Story

As a user navigating the platform, when I click the "TCG Market" logo in the
sidebar or mobile header, I want to be taken to the homepage (dashboard) so I
can quickly return to the main view.

## Dev Notes

### Changes needed

**`frontend/src/components/Layout.tsx`**:

1. **Sidebar logo** (line 84-86): Wrap `<h1>` in `<Link to="/">`:
   ```tsx
   <Link to="/" className="no-underline">
     <h1 className="text-lg font-bold bg-gradient-to-r from-indigo-500 via-purple-400 to-cyan-400 bg-clip-text text-transparent cursor-pointer">
       TEDHC Market
     </h1>
   </Link>
   ```

2. **Mobile header logo** (line 183): Wrap `<span>` in `<Link to="/">`:
   ```tsx
   <Link to="/" className="ml-3 no-underline">
     <span className="text-lg font-bold bg-gradient-to-r from-indigo-500 via-purple-400 to-cyan-400 bg-clip-text text-transparent">
       TEDHC Market
     </span>
   </Link>
   ```

Note: `Link` is already imported from `react-router-dom` in this file.

### Files to modify
- `frontend/src/components/Layout.tsx`

## Testing

- Unit: Render Layout, click sidebar logo text → `navigate("/")` called
- Unit: Render Layout (mobile), click header logo text → `navigate("/")` called
- Unit: Logo text still renders with gradient classes
- Visual: No layout shift when wrapping in Link
