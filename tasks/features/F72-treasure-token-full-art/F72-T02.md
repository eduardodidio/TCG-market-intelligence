# F72-T02 — Frontend Tests

**Status:** planned
**Wave:** 0
**Depends on:** F72-T01

## User Story

As a developer, I want tests covering the treasure modal behavior so regressions are caught.

## Dev Notes

### Files to create/modify

1. **`frontend/tests/components/TreasureModal.test.tsx`** (NEW)
   - Renders image with correct src
   - Displays count
   - Calls onClose on backdrop click
   - Calls onClose on Escape key
   - Uses correct language-aware image

2. **`frontend/tests/components/TreasureBalance.test.tsx`** (MODIFY)
   - Add test: clicking treasure image opens modal
   - Add test: modal receives correct count from balance
   - Verify existing tests still pass

## Testing

- All new tests pass
- All existing TreasureBalance tests pass
- No snapshot regressions
