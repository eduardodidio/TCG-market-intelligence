# F171-T14 — Docs: ADR, README note, diagram sync

- **Wave:** 5
- **Type:** docs
- **Depends on:** F171-T10, F171-T12, F171-T13
- **Status:** planned
- **Files touched:** C `docs/adr/00NN-import-currency-normalization.md`, M `README.md` ⚠, M `docs/diagrams/F171-*.mmd`, maybe M `docs/README.md`

## User Story

As a maintainer, I want the currency-normalization decision and the new import options documented, so that future importers keep `acquisition_price` in BRL.

## Objective

Write the ADR, append the README delivery note, and sync the F171 diagrams with the shipped code.

## Dev Notes

Veja `_brief/00-overview.md` (read all other shards only if a diagram needs detail).

- ⚠ `README.md` is a **batch-wide high-conflict file**. Append one compact section. Do not reflow or reorder existing content.
- ADR numbering: the last ADR is `docs/adr/0013-guest-user-role-system.md`, but F172–F179 may also add ADRs. At write time, run `ls docs/adr` and use the **next free number**. Name: `00NN-import-currency-normalization.md`.
- The ADR states: BRL is the storage currency, conversion happens once at the import boundary with PTAX via `CurrencyConverter.get_display_rate`, no rate means drop the price rather than guess, and detection follows the precedence chain. Alternatives considered: storing the original currency in a DB column (rejected for now because of the `models.py` conflict; possible follow-up) and read-time conversion of mixed currencies (rejected).

- Follow CLAUDE.md: no new dependencies, validate input at boundaries, no secrets, stage files one by one
  (never `git add -A`), never commit to `main`.
- `acquisition_price` is **always BRL**. Never store a USD amount as BRL.

## Implementation details

- README section `### F171 — Import respecting the file currency`: `/collection/import?currency=auto|BRL|USD&dry_run=true`, the new `ImportResult` fields, CsvImportModal currency selector, the purchase import conversion badge, the batch text price token (`R$12,50`, `US$3.10`), CLI `import-csv --currency`, and the `parse_brl_price` 12.50 bug fix.
- Diagrams: open `docs/diagrams/F171-architecture.mmd` and `F171-journey.mmd` and fix any node or edge that drifted from the implementation (e.g. function names, the unmatched `currency_conversion_failed` path).
- Link the ADR and PRD from `docs/README.md` if that index lists them (check first).

## Acceptance criteria

- [ ] ADR exists with the next free number, and status Accepted
- [ ] README has the F171 section (AC10)
- [ ] Diagrams match the code (names of modules/functions verified by grep)
- [ ] The full suite passes: `pytest tests/ --cov=src --cov-report=term-missing`, `ruff check src/`, `cd frontend && npm test && npm run build`

## Testing

Docs task. Run the full backend and frontend suites as a final gate and paste the summary lines into the task summary.

## Test scenarios

- Happy: all suites green.
- Edge: an ADR number collision (another feature took the number) → bump to the next number.
- Error: a Mermaid syntax check. Paste the diagrams into `npx -y @mermaid-js/mermaid-cli` only if it is already available; otherwise do a manual review. **Do not add dependencies.**
- Boundary: the README diff is additive only (`git diff README.md` shows only added lines).

## Diagrams

Updates (sync) `docs/diagrams/F171-architecture.mmd` and `docs/diagrams/F171-journey.mmd`.
