# F171-T01 — Setup: branch check, shared currency types, fixtures, PRD, diagrams

- **Wave:** 0
- **Type:** infra / docs
- **Depends on:** —
- **Status:** planned
- **Maps to AC:** AC9, AC10
- **Files touched:** see README file-ownership table (T01 row)

## User Story

As a developer on later waves, I want the shared types, fixtures, and docs scaffolding in place, so that Waves 1–5 run in parallel without prompting or guessing contracts.

## Objective

Front-load everything the later waves share: branch sanity, `src/currency/types.py`, test fixture exports (BRL and USD), `tests/currency/` package, PRD, and the two F171 diagrams.

## Dev Notes

Veja `_brief/00-overview.md`, `_brief/01-money-parsing.md` and `_brief/03-csv-import.md`.

- **Branch check (learning from F124):** run `git branch --show-current`. The work must be on the
  orchestrator-provided feature branch/worktree (or `homol`), **never `main`**. If on `main`, stop and report.
- `src/currency/__init__.py` already exists (empty). `src/currency/pila_formatter.py` is unrelated; leave it alone.
- Liga CSV reference (real export, CP-1252, 13 columns, **no price column**):
  `docs/colecaoImport/export_1b19325b553f22c3260d042d65c1d7dcb07f2743.csv`. Copy only its header plus 3 rows.
- Diagram templates: `docs/diagrams/templates/architecture.mmd`, `docs/diagrams/templates/user-journey.mmd`.
  Example of a previous feature: `docs/diagrams/F168-architecture.mmd`, `docs/diagrams/F168-journey.mmd`.
- PRD template: `docs/prd/template.md`.

- Follow CLAUDE.md: no new dependencies, validate input at boundaries, no secrets, stage files one by one
  (never `git add -A`), never commit to `main`.
- `acquisition_price` is **always BRL**. Never store a USD amount as BRL.

## Implementation details

1. Create `src/currency/types.py` exactly as specified in `_brief/01-money-parsing.md`
   (`SupportedCurrency`, `CurrencyChoice`, `CurrencySource`, `CurrencyDetection`).
2. Create `tests/currency/__init__.py` (empty).
3. Create fixtures under `tests/fixtures/collection_import/` (UTF-8 unless noted):
   - `liga_brl_no_price.csv`: Liga header + 3 rows, **cp1252 encoded**, with an accented name (e.g. `Irmãos`)
   - `liga_brl_with_price.csv`: Liga header + extra column `Preco (R$)`, values `12,50`, `1.234,56`, `0,75`
   - `generic_brl_symbol.csv`: `Name,Set code,Collector number,Quantity,Price` with values `R$ 10,00`, `R$ 2,50`, `R$ 1.050,00`
   - `generic_usd_symbol.csv`: same columns with `US$ 2.00`, `$0.50`, `$1,050.00`
   - `manabox_usd.csv`: ManaBox-style `Name,Set code,Set name,Collector number,Foil,Rarity,Quantity,Purchase price,Purchase price currency,Condition,Language` with currency `USD`, prices `2.00`, `0.35`
   - `manabox_brl.csv`: same header, currency `BRL`, prices `10.50`, `1.75` (dot decimals, but the column says BRL)
   - `ambiguous_no_hint.csv`: generic header, prices `10`, `3` (no symbol, no separators) → must default BRL
   - Use real card identities (e.g. `Lightning Bolt`, `m10`, `146`; `Sol Ring`, `c21`, `263`).
4. Create `tests/fixtures/purchase_usd_sample.html`: copy the minimal structure of the Liga order markup that
   `parse_liga_orders` expects (see `tests/fixtures/liga_sample.html` and `tests/services/test_purchase_parser.py`), with one item priced `US$ 3.10` and one `R$ 9,75`.
5. PRD `docs/prd/F171-collection-import-currency.md`: problem, users, goals, non-goals, and AC1–AC10 from `_brief/00-overview.md`.
6. `docs/diagrams/F171-architecture.mmd` (flowchart): CSV/HTML/batch text → `csv_columns`/`money` detection →
   `import_conversion.to_brl` (← `CurrencyConverter.get_display_rate` ← `exchange_rates` table) → `user_collection.acquisition_price` (BRL) → read-time `CurrencyConverter.convert` for display.
7. `docs/diagrams/F171-journey.mmd` (flowchart LR with swimlanes User / Frontend / API / DB): select file →
   dry-run detect → show detected currency (low-confidence warning branch) → user keeps or overrides →
   import → no-rate warning branch → success summary.

## Acceptance criteria

- [ ] Current branch is not `main` (reported in the summary)
- [ ] `python -c "from src.currency.types import CurrencyDetection"` works
- [ ] 7 CSV fixtures + 1 HTML fixture exist. `liga_brl_no_price.csv` is cp1252 (it fails `utf-8` decode)
- [ ] PRD and both `.mmd` files exist, and the Mermaid syntax is valid (no unclosed subgraph)

## Testing
- Smoke only (no logic): `python -c "import src.currency.types"` import check.
- Lint: `ruff check src/currency/types.py`.
- Regression: `pytest tests/collection -q` still passes; fixtures exist under `tests/fixtures/collection_import/`.

## Test scenarios

- Happy: types import; a dataclass instance is frozen (assigning raises `FrozenInstanceError`).
- Edge: cp1252 fixture raises `UnicodeDecodeError` under `utf-8`.
- Boundary: `ambiguous_no_hint.csv` contains no `R$`, `$`, `,` or `.` in price cells.
- Error: none (setup task). Later tasks own the behavior tests.

**Test plan:** ver `F171-test-plan.md` (fixtures: liga_brl_no_price.csv, liga_brl_with_price.csv, generic_brl_symbol.csv, generic_usd_symbol.csv, manabox_usd.csv, manabox_brl.csv, ambiguous_no_hint.csv, purchase_usd_sample.html)

## Diagrams

Creates `docs/diagrams/F171-architecture.mmd` and `docs/diagrams/F171-journey.mmd` (owner).
