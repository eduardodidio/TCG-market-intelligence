# F75-T04 — Frontend Tests

**Status:** planned
**Wave:** 2
**Depends on:** F75-T01, F75-T02, F75-T03

## User Story

As a developer, I want tests covering the accordion behavior and section migration.

## Dev Notes

### Files to create/modify

1. **`frontend/tests/components/AccordionSection.test.tsx`** (NEW)
   - Renders title and children
   - Click toggles visibility
   - Chevron rotation
   - Closed state hides children

2. **`frontend/tests/pages/AdminPanel.test.tsx`** (MAJOR UPDATE)
   - Renders 5 accordion sections
   - Only one section open at a time
   - Users section shows user table + create form + delete
   - Dashboard section shows KPI cards
   - Liga section shows coverage data
   - Schedules section shows schedule list
   - Scans section shows scan history
   - Default open section is "users"

3. **`frontend/tests/components/admin/AdminLigaSection.test.tsx`** (NEW)
   - Renders KPIs and table (migrated from AdminLigaStatus tests)

4. **`frontend/tests/components/admin/AdminSchedulesSection.test.tsx`** (NEW)
   - Renders schedule list and form (migrated from Schedules tests)

5. **`frontend/tests/components/admin/AdminScansSection.test.tsx`** (NEW)
   - Renders scan history and trigger (migrated from Scans tests)

6. **Update/remove old page tests:**
   - `frontend/tests/pages/AdminLigaStatus.test.tsx` → migrate or delete
   - `frontend/tests/pages/Schedules.test.tsx` → migrate or delete
   - `frontend/tests/pages/Scans.test.tsx` → migrate or delete

## Testing

- All new tests pass
- All migrated tests pass in new context
- No test references to deleted pages
