# QA Report — F124 (Painel de coleção focado em valorização + fix link Liga)

**Run:** 2026-09-13T21:15:00Z
**Scope:** full feature (Waves 0–3, all tasks T01–T10), validated per `F124-README.md` global ACs.

## Blocker (must resolve before shipping — not a test failure)

All F124 work (55 modified/new paths) is **uncommitted on `main`**. Per `CLAUDE.md`
Gitflow, all feature work must happen on `homol`, promoted to `main` only with
explicit user confirmation. This was already flagged in `F124-wave-3-summary.md`
and remains true. QA did not commit, branch, or push anything — that decision is
left to the user.

## Acceptance criteria

| AC | Description | Verdict | Evidence |
|----|--------------|---------|----------|
| AC1 | Paid price editable from every grid tile; list+detail APIs return it; feeds portfolio/dashboard | PASS | `tests/api/test_collection_acquisition_fields.py` (list+detail null/non-null), `frontend/src/components/__tests__/PaidPriceQuickEdit.test.tsx`, `MyCollectionPaidPrice.test.tsx`, `DashboardInvestmentSummary.test.tsx` |
| AC2 | Clearing paid price persists `null` | PASS | `AcquisitionPriceInput.test.tsx` clear-to-null case; `test_collection_patch.py` |
| AC3 | P&L foil-aware; unpriced excluded + counted in `unpriced_card_count` | PASS | `tests/api/test_collection_portfolio.py::test_foil_entry_uses_foil_price`, `unpriced_card_count` assertions (lines 156–296) |
| AC4 | Dashboard = collection/valuation + Trending only; no `market-summary-strip`/`market-empty`/`/market/stats` | PASS | `frontend/src/pages/__tests__/Dashboard.test.tsx` (asserts both testids absent, investment stub present); verified `Dashboard.tsx` mounts `DashboardInvestmentSummary` + `TrendingSection` only |
| AC5 | Liga link = fetched URL; fallback name matches fetch name; invalid/foreign URLs never served | PASS | `tests/api/test_collection_liga_links.py::test_stored_invalid_url_is_never_served`, fallback-name test; `tests/unit/test_liga_urls.py`, `test_liga_card_urls.py` (validation: empty id/url, length cap) |
| AC6 | `pytest` green, coverage not decreasing; `npm test`+`npm run build` green; `ruff` clean | PASS w/ caveat | See below |
| AC7 | README updated; both `.mmd` diagrams created | PASS | README §"F124 -- Collection Valuation Panel & Liga Link Fix" (line 931) matches implementation; diagrams reviewed against actual endpoints/components — accurate |

## Test suite results

- **`ruff check src/`** — clean, no findings.
- **`pytest tests/ --cov=src --cov-report=term-missing`** — 65 failed, 3371 passed, 90.30% total coverage (≥70% gate). New F124 modules: `src/collectors/liga_url_recorder.py` 100%, `src/providers/liga/urls.py` 91%.
  - All 65 failures are **pre-existing and unrelated to F124** (marketplace, decks, currency, `liga_coverage`, `error_codes`, `seed_users`, `sync_collection`, `provider_windows`, `cards_router` history, `repository_api` pagination) — confirmed against `F124-wave-3-summary.md`'s baseline comparison (`git stash` showed 80 failed on unmodified `main`, only 1 branch-unique failure which was already fixed by T10). No F124-touched test file is in the failing list.
  - **Caveat on AC6**: suite is not fully green because of this pre-existing cluster, tracked separately (many share a `FOREIGN KEY constraint failed` pattern on fresh per-test SQLite DBs — worth its own bug ticket, out of scope for F124).
- **`npm test -- --run`** — 200/201 files, 1957/1958 tests passed. 1 failure: `tests/hooks/useScanStream.test.ts` — reproduced independently via `git stash` on unmodified `main`, confirming pre-existing/unrelated to F124.
- **`npm run build`** — clean, PWA precache generated, no errors.

## New tests added by QA

None — acceptance-criteria coverage was already complete across the Wave 0–3 task deliverables; no gaps found.

## Diagrams

- `docs/diagrams/F124-architecture.mmd` — matches real endpoints/components (`GET/PATCH /collection`, `/collection/portfolio-summary`, `/collection/:id/refresh-liga`, `liga_url_recorder`, `liga_card_urls`).
- `docs/diagrams/F124-journey.mmd` — matches paid-price edit, dashboard view, and Liga link click flows including the stored-URL vs fallback branch.

## Blockers found

1. **Branch/Gitflow** (see top) — all work uncommitted on `main`, not `homol`. Needs explicit user decision before any commit/push.
2. Pre-existing unrelated test failures (backend cluster of 65, frontend `useScanStream`) — not F124's responsibility to fix, but should be tracked as separate tech-debt tickets since they mask real signal in the "all green" gate.

## Final verdict: **PASSED**

All 7 acceptance criteria are met by implementation + tests. The only "not green" results in AC6 are proven pre-existing and unrelated to this feature's changes. Recommend fixing the Gitflow/branch situation with the user before shipping.
