# F124-T08: Slim Dashboard.tsx + mount investment summary

- **Wave:** 2
- **Type:** frontend
- **Depends on:** F124-T04
- **Status:** done
- **Maps to AC:** AC4

## User Story

As a collector, I want the dashboard to show only my collection, its
valuation and market trends, so that I focus on what matters to my investment.

## Objective

Remove market summary strip + market-empty state + `/market/stats` dependency; mount `DashboardInvestmentSummary`.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/02-dashboard.md` (entire shard, especially "Gotcha — loading/error gates").

- File: `frontend/src/pages/Dashboard.tsx` (317 lines). Strip at L238-261, market-empty at L262-277, stats fetch L40, `loading`/`error` L48-49, skeleton branch L71-110, full-page error branch L112-133, `marketStats`/`hasMarketData` L136-138.
- Keep: WelcomeBanner, hero + freshness, `collection-kpis`, `dashboard-movers`, `collection-error`, `collection-empty`, `trending-grid` (both `TrendingSection` with `collectionOnly={isAuthenticated}`), `trending-view-all`.
- Mount `<DashboardInvestmentSummary totalUnique={summaryData.total_unique} />` right after `collection-kpis` grid and before movers, only when `isAuthenticated && summaryData.total_unique > 0`.
- New gates: skeleton when `collectionSummary.loading && !summaryData`; remove the full-page error branch (collection error has its own EmptyState).
- Remove unused imports (`fetchMarketStats`, `MarketStats`, `ErrorBanner` if unused, etc.) — `npm run build` runs tsc.
- Do NOT delete `MarketSummaryKpis.tsx`, `MarketPage.tsx`, backend endpoint or i18n keys.
- No Dashboard page test exists today → create one.

## Implementation details

See shard. Keep section order: Welcome → Hero → Collection KPIs → Investment summary → Movers → Trending → View all.

## Acceptance criteria

- [ ] No `market-summary-strip` / `market-empty` in DOM; `fetchMarketStats` not imported/called by Dashboard.
- [ ] Dashboard renders collection KPIs even if `/market/stats` would fail (no dependency).
- [ ] Investment summary mounted for authenticated users with a non-empty collection.
- [ ] Trending sections still rendered with same props.
- [ ] `npm test` + `npm run build` green.

## Testing

- Vitest + RTL. New `frontend/src/pages/__tests__/Dashboard.test.tsx`.
- Mock modules: `../../api/collection` (`fetchCollectionSummary`, `fetchPortfolioSummary`), `../../api/collect` (`fetchCollectionHealth`), `../../api/market` (spy to assert NOT called), and stub heavy children with `vi.mock("../../components/TrendingSection", ...)`, `CollectionMovers`, `ValuationBadge` returning simple testid divs. Mock `useAuth`, `useWelcome`, `useCurrency` hooks. Follow patterns in `frontend/src/pages/__tests__/MyCollectionSort.test.tsx`.
- Command: `cd frontend && npm test -- Dashboard`.

## Test scenarios

- Happy authenticated with collection: KPIs, `dashboard-investment` stub/real, movers, 2 trending sections, view-all link present; strip absent; `fetchMarketStats` not called.
- Anonymous user: no investment summary; trending rendered with `collectionOnly=false`.
- Empty collection (`total_unique: 0`): `collection-empty` shown, no investment summary, trending still shown.
- Summary error: `collection-error` shown with retry; page not replaced by full-page error.
- Loading: skeleton shown while summary loading.

## Diagrams

None owned (T10).
