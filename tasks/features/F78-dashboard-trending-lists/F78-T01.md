# F78-T01: Dashboard Trending — Compact Lists

**Status:** planned
**Wave:** 0

## User Story
As a user, I want the dashboard to show the top 10 gainers and top 10 losers as compact text lists side by side with percentage changes, without card art, so I can quickly scan market movement.

## Dev Notes

### Current State
- `TrendingSection` has `variant` prop: `"cards"` | `"list"`
- `TrendingListItem` component already renders compact rows (name, set, %change, price, link)
- Dashboard currently uses `TrendingSection` — need to check which variant

### Files to Modify
- `frontend/src/pages/Dashboard.tsx` — change TrendingSection usage to variant="list", pass limit=10
- `frontend/src/components/TrendingSection.tsx` — ensure list variant renders in a 2-column grid (gainers | losers) with `md:grid-cols-2`, no card images
- May need to pass `limit` prop to control number of items (default may be 5, need 10)

### Approach
1. Read current Dashboard.tsx and TrendingSection.tsx to confirm current variant used
2. Switch to variant="list" on Dashboard
3. Ensure TrendingSection list variant shows exactly 10 items per list
4. Layout: two columns side by side at md breakpoint, stacked on mobile
5. Each item shows: rank, card name, set, percentage change (green up / red down), current price

## Testing
- Unit test: TrendingSection with variant="list" renders list items (not card images)
- Unit test: renders exactly 10 items per section when data available
- Unit test: 2-column grid layout at md breakpoint
- Visual: verify on localhost no card art appears in trending section
