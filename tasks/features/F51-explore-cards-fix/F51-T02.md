# F51-T02 — Fix Explore Detail Price Display

**Status:** done
**Wave:** 0
**Estimate:** small

## User Story

As a user viewing card details in explore, when a card has no price data,
I want to see a clear "No price data" message instead of blank space, so
I understand why the price is missing.

## Dev Notes

### What to change

**`frontend/src/pages/CardDetail.tsx`:**
- Ensure the price section always renders — currently may be conditionally
  hidden when `latest_price` is null
- Show `t("common.noPriceData")` in slate-500 when price is null (match
  the pattern in CardTile)
- Show currency indicator (CurrencyIndicator component) alongside price
- Ensure price converts correctly with the selected currency

### Files to modify
- `frontend/src/pages/CardDetail.tsx`

### Files to reference
- `frontend/src/components/CardTile.tsx` (price display pattern)
- `frontend/src/utils/format.ts` (formatPriceOrFallback)

## Testing

- Unit test: CardDetail shows "No price data" when latest_price is null
- Unit test: CardDetail shows formatted price with currency indicator
