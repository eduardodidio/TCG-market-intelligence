# F79-T02: Backend Verification + i18n Keys

**Status:** planned
**Wave:** 0

## User Story
As a developer, I want to verify the backend total_value calculation multiplies by quantity correctly and add i18n keys for the new display pattern.

## Dev Notes

### Files to Verify
- `src/database/repository.py` — `get_collection_total_value` — confirm it does `SUM(median_price * quantity)` not just `SUM(median_price)`
- `src/api/routers/collection.py:167` — confirm `total_value` in summary response reflects qty-weighted sum

### Files to Modify
- `frontend/src/i18n/locales/en.json` — add keys: `collection.unitPrice`, `collection.lineTotal`, `collection.qtyMultiplier`
- `frontend/src/i18n/locales/pt-BR.json` — same keys in Portuguese

### i18n Keys
```json
{
  "collection.qtyMultiplier": "{{price}} x {{qty}} = {{total}}",
  "collection.qtyMultiplierPt": "{{price}} x {{qty}} = {{total}}"
}
```

## Testing
- Backend unit test: `get_collection_total_value` with entries having qty=1 and qty=3, assert total = sum(price*qty)
- Verify existing tests still pass
