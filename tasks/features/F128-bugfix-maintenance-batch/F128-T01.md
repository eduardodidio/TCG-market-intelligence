# F128-T01: Remove PWA UpdatePrompt Modal

**Status:** planned
**Wave:** 0
**Depends on:** none

## User Story

As a user, I don't want to see a "new version available" toast that
causes confusion. The app should auto-update silently on reload.

## Dev Notes

### What to remove

1. **Delete** `frontend/src/components/UpdatePrompt.tsx` (39 lines)

2. **Edit** `frontend/src/components/Layout.tsx`:
   - Remove import on line 19: `import { UpdatePrompt } from "./UpdatePrompt";`
   - Remove render on line 412: `<UpdatePrompt />`

3. **Edit** `frontend/src/components/__tests__/Layout.test.tsx`:
   - Remove mock on lines 80-81:
     ```ts
     vi.mock("../UpdatePrompt", () => ({
       UpdatePrompt: () => null,
     }));
     ```

### What to keep

- `vite-plugin-pwa` config in `vite.config.ts` — SW still registers and
  auto-updates on next navigation
- `OfflineBanner` and `InstallPrompt` — separate components, unaffected
- `useRegisterSW` is only used inside `UpdatePrompt.tsx` — safe to remove

### Acceptance Criteria

- [ ] No "update available" toast appears after deploy
- [ ] PWA still installs correctly (InstallPrompt unaffected)
- [ ] OfflineBanner still works
- [ ] Build succeeds with no dead imports
- [ ] All existing Layout tests pass

## Testing

- Verify `npm run build` succeeds (no dead import errors)
- Verify Layout test suite passes (mock removed cleanly)
- Manual: deploy → refresh → no update toast
