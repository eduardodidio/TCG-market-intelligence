# F71-T05 — Tests

**Status:** planned
**Wave:** 1
**Depends on:** T01

## User Story

As a developer, I need comprehensive tests for the treasure token image feature to ensure language-aware image selection, i18n text, and visual rendering work correctly across all components.

## Dev Notes

1. **`useTreasureImage` hook tests** (`frontend/tests/hooks/useTreasureImage.test.ts`):
   - Mock LanguageContext with 'en' -> returns treasure.jpg asset URL
   - Mock LanguageContext with 'pt-BR' -> returns tesouro.png asset URL
   - No context (null) -> defaults to EN image

2. **TreasureTokenCard tests** (update existing `frontend/tests/components/TreasureTokenCard.test.tsx`):
   - Renders `<img>` element with correct src for EN
   - Renders `<img>` element with correct src for PT-BR
   - Count badge still displays correctly
   - Type line shows localized text
   - Rules text shows localized text

3. **TreasureBalance tests** (update existing `frontend/tests/components/TreasureBalance.test.tsx`):
   - Renders `<img>` element instead of "T" text
   - Image src changes with language
   - Loading skeleton has no img

4. **CreditConfirmModal tests** (update existing):
   - Verify TreasureTokenCard inside modal renders with image
   - No new logic needed — just ensure no regression

## Testing

- All tests use `@testing-library/react` + vitest
- Mock image imports via vitest's asset transform (should work out of the box)
- Mock LanguageContext via wrapper component
