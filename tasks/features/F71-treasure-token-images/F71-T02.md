# F71-T02 — TreasureTokenCard Real Images

**Status:** planned
**Wave:** 1
**Depends on:** T01

## User Story

As a user, I want to see the real MTG Treasure Token card art on the treasure token card component, matching my selected language (English art for EN, Portuguese art for PT-BR), so the credit system feels thematic and immersive.

## Dev Notes

1. **Edit `frontend/src/components/TreasureTokenCard.tsx`**:
   - Import `useTreasureImage` from the new hook
   - Replace the CSS gradient art box (lines 22-28, the `bg-gradient-to-br` div with emoji) with an `<img>` tag using the hook's URL
   - Keep the count badge overlay (absolute positioned, bottom-right)
   - Keep the card frame structure (name bar, art box, type line, text box)
   - The image should fill the art box area with `object-cover`

2. **Replace hardcoded text** in type line and text box with i18n keys (coordinated with T04):
   - Type line: `t("credits.tokenTypeLine")` instead of `"Token Artifact — Treasure"`
   - Text box: `t("credits.tokenRulesText")` instead of the hardcoded quote

3. **Responsive**: image should maintain aspect ratio within the card frame. Use `w-full h-28 object-cover` on the img.

## Testing

- Snapshot or render test: verify `<img>` is rendered with correct src based on language
- Verify count badge still renders with correct value
- Verify the card structure (name bar, art, type line, text box) is maintained
