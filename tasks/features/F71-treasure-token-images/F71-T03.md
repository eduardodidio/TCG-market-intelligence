# F71-T03 — TreasureBalance Image Thumbnail

**Status:** planned
**Wave:** 1
**Depends on:** T01

## User Story

As a user, I want to see a small treasure token card image in the sidebar balance widget instead of a plain "T" circle, so the credit indicator is visually recognizable as a treasure token.

## Dev Notes

1. **Edit `frontend/src/components/TreasureBalance.tsx`**:
   - Import `useTreasureImage` from the new hook
   - Replace the CSS gradient circle (lines 36-41, the `w-8 h-8 rounded-full bg-gradient-to-br` div with "T" span) with a small `<img>` element
   - Use `w-8 h-8 rounded-full object-cover border-2 border-yellow-300 shadow-lg` for the image styling
   - The image should be a circular crop of the treasure token art

2. **Loading state**: keep the existing pulse skeleton for loading state (no image needed there)

3. **Accessibility**: add `alt={t("credits.balance")}` to the img tag

## Testing

- Render test: verify `<img>` element is present with correct src
- Verify image changes when language context switches
- Verify loading skeleton still works (no img in loading state)
