# F172-T01 — Setup: branch check, scaffolding, PRD, ADR

- **Wave:** 0
- **Type:** docs / infra
- **Depends on:** —
- **Status:** done
- **Maps to AC:** AC10

## User Story

As the development team, I want the branch, directories, PRD and ADR ready before coding starts, so that every later Wave
can run unattended and follow the documented decision.

## Objective

Confirm the working branch, create the new package/test/component directories, and write the PRD and ADR.

## Dev Notes

See `_brief/00-overview.md` and `_brief/05-integration-docs.md` (read `_brief/03-claude-processor.md` for the ADR content).

- CLAUDE.md Gitflow: feature work happens on `homol` or the orchestrator's worktree branch. **Never** `main`.
  Run `git branch --show-current`. If it is `main`, stop and report.
  The orchestrator worktree branch `wt/F172` is VALID (it merges into `homol`). Do **not** switch branches.
- PRD template: `docs/prd/template.md`. Existing examples: `docs/prd/F23-deck-import.md`.
- ADRs: `docs/adr/`. This feature's ADR number is **reserved: 0015** (batch reservation in `tasks/features/EXECUTION-PLAN-F171-F179.md`).
  Write `docs/adr/0015-deck-suggestion-queue-claude.md`. Do not pick another number.
- No dependency installs are needed (no new deps). `httpx` already exists in `pyproject.toml`.

## Implementation details

1. `src/deck_suggestions/__init__.py`: one-line docstring `"""F172 — deck suggestion queue processed by Claude."""`.
2. `tests/unit/deck_suggestions/__init__.py`: empty.
3. `frontend/src/components/decks/.gitkeep`: empty (T06 removes it when adding the first component. Optional).
4. `docs/prd/F172-deck-builder-suggestions.md`: problem, users, goals and non-goals, user stories for both modes,
   functional requirements (AC1–AC10 of `_brief/00-overview.md`), data model summary, API table, ops routine, risks
   (prompt injection via notes, Claude cost, hallucinated card names → the unresolved list), success metrics.
5. `docs/adr/0015-deck-suggestion-queue-claude.md`: Context / Decision / Alternatives
   (sync generation in the request = rejected: latency, and Render has no Claude access. `anthropic` SDK = rejected (user decision 2026-09-24): no new dep; default provider is `claude -p`.
   In-process scheduler = rejected: follows F09 learning) / Consequences.

## Acceptance criteria

- [x] Branch is not `main` (reported in the output).
- [x] 3 scaffolding files exist.
- [x] The PRD covers both modes, the table, the API, the daily routine, and risks.
- [x] The ADR is `0015-deck-suggestion-queue-claude.md` and records the "no SDK, httpx + CLI" and "own-module table" decisions.

## Testing

- No code in this task, so there are no new tests.
- Verify the package imports: `python -c "import src.deck_suggestions"`.
- Run `pytest tests/unit/deck_suggestions -q` (0 tests collected is OK).
- Verify the ADR file exists: `ls docs/adr/0015-deck-suggestion-queue-claude.md`.
- `ruff check src/` must be clean.

## Test scenarios

- Happy: the package imports.
- Edge: `docs/adr/0015-*` already exists from another feature → stop and report (0015 is reserved for F172).
- Error: the branch is `main` → abort without writing.
- Boundary: n/a.

**Test plan:** ver `F172-test-plan.md` (fixtures: none — docs/infra task, no fixture obligation)

## Diagrams

None (T16 owns the F172 diagrams).

## Notes from Developer

- Branch verified: `wt/F172` (orchestrator worktree, not `main`).
- `docs/adr/0015-*` did not exist before this task, so the reserved number was free.
- Also read `_brief/02-suggestion-persistence-api.md` (not cited) because the PRD must include the data model and API table.
- In this environment `pytest-cov` is not installed, so the `pyproject.toml` addopts (`--cov…`) make plain `pytest` fail with a usage error.
  Verified with `pytest tests/unit/deck_suggestions -q -o addopts=""` (0 tests collected, as expected).
- `frontend/src/components/decks/` was empty. `.gitkeep` added (T06 removes it).
