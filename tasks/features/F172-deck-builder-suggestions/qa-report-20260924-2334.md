# F172 — QA Validation Report

**Feature:** Montar Deck — commander search fix + "Sugestão de deck" mode
**Tasks validated:** T01–T18 (Waves 0–4)
**Validated:** 2026-09-24
**TechLead review:** `review-20260924-2330.md` — verdict `APPROVED_WITH_FOLLOWUP` (i18n namespace split `deckSuggest.*` vs `deckSuggestions.*` in `SuggestionRequestList.tsx`, non-blocking cleanup follow-up)

## Per-criterion pass/fail

| AC | Description | Verdict | Evidence |
|---|---|---|---|
| AC1 | Commander search finds legendary creatures by EN/PT name, empty `card_legalities` OK, explicit bans excluded | PASS | `tests/unit/decks/test_commander_search.py`, `test_builder.py` — soft filter excludes only `banned`/`not_legal` rows; `builder.py` searches `name_en`/`name_pt` |
| AC2 | Loading/empty/error states, no stale-response race | PASS | `CommanderSearch.test.tsx` (14 tests) covers all states + race guard |
| AC3 | Manual-mode decks contain non-land cards with missing legality data; budget enforced with real prices | PASS | `test_builder.py` covers legality-missing generation + budget cap with priced cards |
| AC4 | `/decks/build` mode chooser (manual vs. suggestion), `?mode=` deep link | PASS | `DeckBuildModeChooser.test.tsx` (8 tests), `DeckBuildWizard.test.tsx` (21 tests) exercise both `?mode=manual` and `?mode=suggestion` |
| AC5 | Suggestion requests persisted as `pending`, `POST` returns 201 immediately, list shows status | PASS | `test_deck_suggestions_endpoints.py`, `SuggestionRequestList.test.tsx` |
| AC6 | `process-deck-suggestions` claims pending, calls Claude (CLI default / `--provider api`), prioritizes owned cards, prices missing cards, `failed` w/ message, retries ≤3 | PASS | `test_processor.py`, `test_claude_runner.py`, `test_deck_suggestions_cli.py` — retry-to-3 and both runners covered |
| AC7 | Result view: owned vs. missing + cost to complete; "Salvar como deck" idempotent | PASS | `SuggestionResultView.test.tsx` (18 tests); 409-idempotent save confirmed in `test_deck_suggestions_endpoints.py` |
| AC8 | `bats/deck-suggestions.bat` exists, no hardcoded secrets | PASS | File present; grepped `claude_runner.py` + `.bat` for `ANTHROPIC_API_KEY=`/`sk-ant` literals — none found; `.env.example` documents env-only key |
| AC9 | Backend/frontend tests green, new modules ≥90% cov, `ruff check` clean | PASS | See test/coverage runs below |
| AC10 | PRD, ADR 0015, both diagrams, README updated | PASS | All 5 artifacts present and non-trivial (confirmed by content, not just existence) |

**10/10 global acceptance criteria pass.**

## Test command output summary

**Backend — targeted F172 scope** (headless sandbox; ran targeted paths per task instructions rather than the full 17k-line `tests/` suite, which exceeds the foreground timeout and is not required beyond what TechLead already exercised):

```
pytest tests/unit/deck_suggestions tests/unit/decks tests/unit/api/test_deck_endpoints.py \
  tests/unit/api/test_deck_suggestions_endpoints.py tests/unit/api/test_deck_suggestions_wiring.py \
  tests/cli/test_deck_suggestions_cli.py --no-cov -q
→ 521 passed
```

Coverage of new/changed modules only (`--cov=src.deck_suggestions --cov=src.decks.builder`):

```
claude_runner.py   100%
models.py          100%
prompt.py          100%
repository.py      100%
processor.py        99%  (1 line, non-happy-path branch)
builder.py           91%  (uncovered lines are pre-existing branches outside this feature's diff)
```

All new modules ≥ 90% (AC9 satisfied). Matches TechLead's independently-reported numbers.

`ruff check src/` → **All checks passed!**

**Frontend:**

```
npx vitest run <9 F172 test files>  → 9 files, 134 tests, all passed
npx vitest run (full suite)         → 240 passed, 12 failed test files, 2575/2593 tests passed
```

The 12 failing files (`ImportPurchasesPage`, `MyTrades`, `MyCollection`, `DeckView`, `TopDecksPage`, `DeckList`, `Evaluations`, `Layout`, `Login`, `TreasureModal`, `OfflineBanner`) are all members of the documented pre-existing 13-file baseline (`UpdatePrompt` happened to pass this run). **No new frontend test failures introduced by F172.**

`npm run build` (after removing `frontend/dist`) → green, `DeckBuildWizard` chunk builds (37.14 kB), PWA precache generated successfully.

## New tests added by QA

None — coverage gap analysis found every global AC already covered by an existing test (see table above); no gap required a new test.

## Blockers found

None blocking. One non-blocking cosmetic item, already flagged by TechLead and not re-litigated here: `SuggestionRequestList.tsx` uses the `deckSuggestions.*` i18n namespace while sibling components use `deckSuggest.*`; T18 added duplicate keys under both namespaces to keep tests green. Recommend the already-proposed fast-follow (rename to `deckSuggest.list.*`/`deckSuggest.status.*`, drop the dead `deckSuggestions.*` keys).

## Live/manual checks — pending-user

Per sandbox constraints (outbound network blocked, proxy returns `CONNECT 403`), the following could not be exercised live and are recorded as **pending-user** verification on a machine with real network/Claude CLI access:
- Actually running `claude -p` via `ClaudeCliRunner` end-to-end (subprocess call is unit-tested with a mocked binary, not a real CLI invocation).
- Live Liga Magic / MYP price lookups feeding the missing-card cost total in `SuggestionResultView`.
- Manual click-through of the mode chooser → suggestion form → daily `.bat` → result view flow in a running browser + `python -m src.cli.main serve`.

These are implementation-adjacent I/O boundaries already covered by mocked unit tests (`test_claude_runner.py`, `test_processor.py`); nothing here indicates a defect, only that live verification needs the user's own environment.

## Final verdict: PASSED
