# F172-T18 — i18n (shared files): `deckBuild.*` + `deckSuggest.*` in en / pt-BR

- **Wave:** 4
- **Type:** frontend
- **Depends on:** F172-T06, F172-T10, F172-T11, F172-T12, F172-T13, F172-T14
- **Status:** done
- **Maps to AC:** AC2, AC4, AC9

## User Story

As a Brazilian player, I want the deck builder and the suggestion screens in Portuguese, so that the app feels native.

## Objective

Add every i18n key used by the wizard (`deckBuild.*`, missing today, defect H9) and by the new components (`deckSuggest.*`) to both locale files.

## Dev Notes

See `_brief/00-overview.md`, `_brief/01-commander-search-fix.md` (H9) and `_brief/05-integration-docs.md`.

- Locales: `frontend/src/i18n/locales/en.json`, `frontend/src/i18n/locales/pt-BR.json`. There is an existing `deckBuilder` object (~L451 pt-BR). **Do not rename it.**
  Add new sibling objects `deckBuild` and `deckSuggest`.
- Collect the keys with: `grep -rhoE "t\(\"(deckBuild|deckSuggest)\.[A-Za-z0-9_.]+\"" frontend/src | sort -u`.
  The English value = the `defaultValue` in the code. pt-BR = a natural translation. The project uses gaucho flavor in some strings (F54). Keep it neutral here unless the neighbors do otherwise.
- **Batch conflict rule:** other F171–F179 features may edit these JSON files. Insert the new top-level keys (don't reorder existing ones) and keep the formatting (2-space indent).
- If there is an i18n key-parity test (`frontend/src/i18n/__tests__/`), it must pass. Read it first.

## Implementation details

Both files get identical key sets for `deckBuild` and `deckSuggest` (nested: `deckSuggest.mode.*`, `.form.*`, `.list.*`, `.status.*`, `.result.*`).

## Acceptance criteria

- [ ] Every `deckBuild.*` / `deckSuggest.*` key used in `frontend/src` exists in both files.
- [ ] Both JSON files are valid, and the existing i18n tests pass.
- [ ] `npm test` and `npm run build` are green.

## Testing

- Targeted: `cd frontend && npx vitest run src/i18n`.
- Then `cd frontend && npm test` and `npm run build`.
- Add a small test `src/i18n/__tests__/deckSuggestKeys.test.ts` that loads both JSON files and asserts the `deckBuild` / `deckSuggest` key sets are equal and non-empty (skip it if a generic parity test already covers all namespaces).

## Test scenarios

- Happy: key parity en ↔ pt-BR.
- Edge: nested keys (`deckSuggest.status.pending`) are present.
- Error: a missing key in pt-BR → the test fails (verify by temporarily removing one key locally, then restore it).
- Boundary: interpolation placeholders (`{{query}}`) are identical in both languages.

**Test plan:** ver `F172-test-plan.md` (fixtures: none — key-parity assertions read the locale JSON files directly)

## Diagrams

None.

## Notes from Developer

- Added three new top-level objects right after `deckBuilder` (not renamed) in both locale files: `deckBuild` (22 keys), `deckSuggest` (`mode`, `form`, `panel`, `result`; 33 keys) and **`deckSuggestions`** (`list`, `status`; 12 keys).
- **Assumption / deviation:** `SuggestionRequestList.tsx` (T12) uses the plural namespace `deckSuggestions.list.*` / `deckSuggestions.status.*` instead of `deckSuggest.list.*` / `.status.*` as planned. I didn't edit that component because it belongs to an earlier task. I added the keys under the namespace the code actually uses so the list gets translated. The TechLead may rename them later.
- Some code `defaultValue`s are in Portuguese (SuggestionResultView, DeckSuggestionPanel, SuggestionRequestList). For these, `en.json` gets a real English translation and pt-BR keeps the Portuguese default. `deckBuild.title` has two defaults ("Build Deck" / "Deck Builder"). I used "Deck Builder".
- Placeholders: only `deckSuggestions.list.ownedOfTotal` has any (`{{owned}}/{{total}}`). They're identical in both languages, and the test asserts this.
- The new test `src/i18n/__tests__/deckSuggestKeys.test.ts` (7 tests) checks: key parity, that every used key is present, nested keys, no empty values, placeholder parity, and that `deckBuilder` still exists. I confirmed it fails when a pt-BR key is removed (2 tests fail), then restored the key.
- `npm run build`: green. `npm test`: 18 failures in 12 unrelated files (Layout, OfflineBanner, UpdatePrompt, TreasureModal, DeckList, DeckView, Evaluations, Login, MyCollection, MyTrades, TopDecksPage, ImportPurchasesPage). They already fail without this task: `git stash push -- src/i18n/locales/en.json src/i18n/locales/pt-BR.json` followed by running those 12 files gives `Tests  18 failed | 141 passed (159)`, the same result as with the change. The deck/i18n suites all pass (110 + 13 tests).
