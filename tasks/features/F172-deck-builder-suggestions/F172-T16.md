# F172-T16 — Docs: architecture + journey diagrams

- **Wave:** 3
- **Type:** docs
- **Depends on:** F172-T08, F172-T09
- **Status:** planned

## User Story

As a maintainer, I want up-to-date diagrams of the deck-builder fix and the suggestion pipeline, so that I can understand the flow without reading all the code.

## Objective

Create `docs/diagrams/F172-architecture.mmd` and `docs/diagrams/F172-journey.mmd`.

## Dev Notes

See `_brief/00-overview.md`, `_brief/02-suggestion-persistence-api.md` and `_brief/03-claude-processor.md`.

- Templates: `docs/diagrams/templates/architecture.mmd`, `docs/diagrams/templates/user-journey.mmd`. Recent example: `docs/diagrams/F85-*.mmd`.
- Check the actual names in the implemented code (T02–T09) and keep the diagrams in sync with them.
- Validate the syntax: if `npx @mermaid-js/mermaid-cli` is not installed, **do not install it** (no new deps). Review it manually and keep the syntax simple.

## Implementation details

Architecture stub (refine it):

```mermaid
flowchart LR
  subgraph FE[React SPA]
    W[DeckBuildWizard ?mode] --> MC[DeckBuildModeChooser]
    W --> CS[CommanderSearch]
    W --> P[DeckSuggestionPanel]
    P --> F[SuggestionRequestForm] --> CS
    P --> L[SuggestionRequestList]
    P --> R[SuggestionResultView]
  end
  subgraph API[FastAPI /api/v1]
    DC[GET /decks/commanders] --> B[builder.get_commander_candidates]
    DS[/deck-suggestions CRUD + save/]
  end
  subgraph DB[(Neon PG / SQLite)]
    T[(deck_suggestion_requests)]; C[(cards)]; UC[(user_collection)]; PO[(price_observations)]; D[(decks / deck_cards)]
  end
  subgraph LOCAL[Local Windows daily]
    BAT[bats/deck-suggestions.bat] --> CLI[process-deck-suggestions] --> PR[processor]
    PR --> PM[prompt.py] ; PR --> RN[claude_runner]
    RN -->|claude -p| CC[Claude CLI]
    RN -->|httpx + ANTHROPIC_API_KEY| AA[Anthropic API]
  end
  CS --> DC ; F --> DS ; L --> DS ; R --> DS
  DS --> T ; DS -->|save| D ; B --> C
  PR --> T ; PR --> UC ; PR --> C ; PR --> PO
```

Journey: `flowchart LR` with swimlanes `Usuário`, `App (SPA/API)`, `Rotina diária`, `Claude`. Include the decisions:
the mode choice, commander vs 60-card format, validation error / limit of 5 open requests, "pedido registrado",
the daily run → Claude OK / transient error (retry ≤3) / invalid response (failed), the user returns → views the result → saves as a deck → DeckView.
Also include the manual path: commander search → empty/error states → generate → review.

## Acceptance criteria

- [ ] Both files exist and render in the GitHub Mermaid preview (manual check of the syntax: no reserved words as IDs, and quote labels that contain special chars).
- [ ] Every component/table/command named in the diagrams exists in the code.

## Testing

Manual review. Optional: `grep` every node name against `src/` and `frontend/src/`.

## Test scenarios

- Happy: the diagrams render.
- Edge: labels with `?`, `/` or `:` are quoted.
- Error: n/a.
- Boundary: n/a.

## Diagrams

Owner of `docs/diagrams/F172-architecture.mmd` and `docs/diagrams/F172-journey.mmd`.
