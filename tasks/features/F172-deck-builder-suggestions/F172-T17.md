# F172-T17 — Integration (shared files): router + CLI registration, daily .bat, .env.example, README

- **Wave:** 4
- **Type:** infra
- **Depends on:** F172-T08, F172-T15 (and all of Waves 1–3 for the README content)
- **Status:** planned

## User Story

As the operator, I want the new endpoints and command wired in, plus a daily .bat, so that the feature works end to end locally and on Render.

## Objective

Make the minimal edits to the batch-shared files, all isolated in this one task.

## Dev Notes

See `_brief/00-overview.md`, `_brief/03-claude-processor.md` (bat section) and `_brief/05-integration-docs.md` (table of exact changes).

- **Batch conflict rule:** F171–F179 also edit these files. Keep the edits tiny and append-style. Rebase-friendly: add lines next to the neighbors,
  and do not reorder or reformat existing code. Stage each file individually (`git add <file>`; never `-A` / `.`).
- `src/api/app.py`: find the router import block (`from src.api.routers.decks import router as decks_router` or similar) and add
  `from src.api.routers.deck_suggestions import router as deck_suggestions_router`. Add
  `app.include_router(deck_suggestions_router, prefix="/api/v1")` right after `decks_router` (~L378).
- `src/cli/main.py`: add just above `if __name__ == "__main__":`
  ```python
  from src.cli.deck_suggestions import process_deck_suggestions  # noqa: E402  (F172)

  cli.add_command(process_deck_suggestions)
  ```
- `bats/deck-suggestions.bat`: mirror `bats/process-queue.bat` (it `cd`s to the repo root and prints a date banner).
  The command is `python -m src.cli.main process-deck-suggestions --limit 10`. Include REM lines about the Windows Task Scheduler daily trigger (e.g. 03:00),
  that `.env` is auto-loaded by `src/config.py`, the `DECK_SUGGEST_*` vars, and that `ANTHROPIC_API_KEY` goes ONLY in `.env` when `DECK_SUGGEST_PROVIDER=api`.
  Use CRLF line endings, like the other .bat files (check with `file bats/process-queue.bat`).
- `.env.example`: append a commented block:
  ```
  # --- F172 deck suggestions (local daily routine) ---
  # DECK_SUGGEST_PROVIDER=cli        # cli (claude -p, default) | api (Anthropic HTTP API)
  # DECK_SUGGEST_CLAUDE_BIN=claude
  # DECK_SUGGEST_MODEL=claude-sonnet-5
  # DECK_SUGGEST_TIMEOUT=300
  # ANTHROPIC_API_KEY=               # only for DECK_SUGGEST_PROVIDER=api — never commit
  ```
- `README.md`: in "Shipped", append `### F172 -- Montar Deck: fix busca de comandante + Sugestão de deck (<date>)` with bullets:
  the commander search fix (PT names, legality soft-filter), budget prices, the mode chooser (`/decks/build?mode=`), the endpoints (5),
  the table, the CLI command, the .bat, the env vars, and the pending decision about the Anthropic SDK. Also add the endpoints to "REST API → Endpoints" and the command to "Commands" if those lists are maintained.

## Implementation details

Also create `tests/unit/api/test_deck_suggestions_wiring.py`, which asserts that:
(1) `create_app()` (or the factory used in `tests/unit/api/test_app_lifespan.py`; copy its approach) exposes `/api/v1/deck-suggestions` in `app.routes`;
(2) `from src.cli.main import cli` → `"process-deck-suggestions" in cli.commands`.

## Acceptance criteria

- [ ] `python -m src.cli.main process-deck-suggestions --help` works.
- [ ] `python -m src.cli.main process-deck-suggestions --dry-run` works against a local SQLite DB.
- [ ] The OpenAPI (`/docs`) lists the 5 `/api/v1/deck-suggestions` routes.
- [ ] The .bat exists with CRLF line endings and no secrets.
- [ ] The README note is present. `ruff check src/` is clean. The full `pytest tests/` is green.

## Testing

`pytest tests/unit/api/test_deck_suggestions_wiring.py -q` and then the full `pytest tests/ --cov=src --cov-report=term-missing`. `ruff check src/`.

## Test scenarios

- Happy: the route is registered; the command is registered.
- Edge: importing `src.cli.main` doesn't raise a circular import (the test itself covers this).
- Error: n/a.
- Boundary: `/api/v1/deck-suggestions` does not shadow `/api/v1/decks/{deck_id}` (GET `/api/v1/decks/ranking` is still OK. Assert that both paths exist).

## Diagrams

None (T16).
