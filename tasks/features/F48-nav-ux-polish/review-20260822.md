# F48 Nav & UX Polish -- Tech Lead Review

**Date:** 2026-08-22
**Reviewer:** Tech Lead (automated)

## Summary

Two surgical frontend fixes, both correctly scoped and minimal.

## T01: Nav Active State Fix

**Change:** `Layout.tsx` line 128 -- replaced `startsWith(item.to)` ternary with `location.pathname === item.to`.

**Assessment:** APPROVED. The fix is correct because every navigable route has its own entry in `NAV_ITEMS`. Exact match eliminates the prefix-collision bug (`/market` highlighting `/market/trending`, etc.) without introducing any new edge cases. The old `"/"` special-case is now redundant and removed, simplifying the code.

**Tests:** 6 new tests cover all prefix-collision pairs plus the two baseline routes (`/` and `/collection`). Tests use `bg-indigo-500` class as the active indicator, which matches the production styling.

## T02: Default Sort Price/Desc

**Change:** `MyCollection.tsx` -- default sort changed from `name/asc` to `price/desc`, URL param sync conditions updated to match new defaults.

**Assessment:** APPROVED. Both the state initialization (lines 222-223) and URL sync (lines 363-364) were updated consistently. The `buildParams` function already handles `sortBy === "price"` by skipping server-side sort params, so no backend changes needed.

**Tests:** 4 new tests verify default selection, URL override, clean URLs for defaults, and URL params for non-defaults.

## Risk Assessment

- **Low risk.** Both changes are isolated to single files with no cross-component dependencies.
- No new dependencies introduced.
- No backend changes.
- Pre-existing test failure in `useScanStream.test.ts` is unrelated (SSE error handling race condition).

## Verdict

**SHIP IT.** Both tasks are correctly implemented with comprehensive test coverage.
