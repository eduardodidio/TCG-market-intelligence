# F77-T03: Parser Tests with Real Liga HTML Fixtures

**Status:** planned
**Wave:** 0

## User Story
As a developer, I want regression tests using real Liga HTML snapshots so parser changes don't silently break price extraction.

## Dev Notes

### Files to Create/Modify
- `tests/fixtures/liga/` — save 2-3 real Liga card page HTML samples (anonymized if needed)
- `tests/providers/liga/test_parser.py` — add parametrized tests loading fixtures and asserting correct price extraction

### Approach
1. Capture HTML from a working Liga card page (via Playwright or manual save)
2. Save as test fixture
3. Write tests asserting `parse_card_prices(fixture_html, "Card Name")` returns expected `normal.mid`, `normal.low`, etc.

## Testing
- Parametrized pytest with at least 2 fixtures (card with prices, card with no prices)
- Assert exact Decimal values for known cards
