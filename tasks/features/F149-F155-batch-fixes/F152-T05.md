# F152-T05: CANCELLED — Frontend timeout already exists

**Feature:** F152 — Dashboard movers timeout fix
**Wave:** N/A
**Status:** cancelled

## Reason

Investigation revealed that the frontend API client (`client.ts`) already
has a `DEFAULT_TIMEOUT_MS = 10_000` (10s) timeout via AbortController.
`fetchCollectionMovers()` uses the default timeout. The CollectionMovers
component already has error state + retry button (added in F144).

The real fix is backend-only (T02). No frontend changes needed.
