# F153-T06: Remove catalog nav entry + route

**Feature:** F153 — Remove catalog page (merge into explore)
**Wave:** 1
**Status:** planned

## User Story

As a user, I want a single "Explorar Cartas" entry point instead of
two separate pages (Explorar + Catalogo), to simplify navigation.

## Dev Notes

### File: `frontend/src/components/Layout.tsx`

Remove from `PRIMARY_NAV_ITEMS` array (line 67):
```typescript
// DELETE this line:
{ to: "/catalog", labelKey: "nav.catalog", requiresAuth: false, icon: ICONS.book },
```

### File: `frontend/src/App.tsx`

Replace the `/catalog` route (lines 294-302) with a redirect:
```tsx
<Route path="/catalog" element={<Navigate to="/cards" replace />} />
```

This preserves bookmarks — anyone visiting /catalog gets redirected to /cards.

### DO NOT delete

- `frontend/src/pages/CatalogPage.tsx` — keep for reference
- Any backend catalog endpoints — keep for API compatibility
- Database catalog data — keep all cards

### Clean up lazy import

Remove the `CatalogPage` lazy import from App.tsx (lines 86-88) since
the route now uses Navigate instead.

## Testing

- Unit test: Layout sidebar does not render catalog nav item
- Unit test: /catalog redirects to /cards
- Verify build succeeds (no dead imports)
- Manual: confirm /cards still shows all explore functionality

## Acceptance Criteria

- [ ] No "Catalogo de Cartas" in sidebar navigation
- [ ] /catalog URL redirects to /cards
- [ ] All catalog data remains in database
- [ ] Backend catalog API endpoints still functional
- [ ] Build passes with no unused import warnings
