# F63-T01: Update project name across config, docs, frontend

**Status:** done
**Wave:** 0
**Parallel with:** F64-T01, F68-T01, F70-T01

## User Story

As a project maintainer, I want the project to be internally branded as
"TEDHC Market" (Trading Elder Dragon Highlander Cards) so that documentation
and API reflect our identity accurately.

## Dev Notes

### Changes needed

1. **`src/api/app.py`** line 138: Change `title="TCG Market Intelligence API"`
   to `title="TEDHC Market API"`

2. **`CLAUDE.md`**: Update `Project: **TCG Market Intelligence**` to
   `Project: **TEDHC Market** (Trading Elder Dragon Highlander Cards)`

3. **`README.md`**: Update project title and first paragraph

4. **`frontend/package.json`**: Update `"name"` field to `"tedhc-market"`

5. **`frontend/src/components/Layout.tsx`**:
   - Line 85: Change `TCG Market` to `TEDHC Market` in sidebar h1
   - Line 183: Change `TCG Market` to `TEDHC Market` in mobile header span

6. **`frontend/src/pages/Login.tsx`**: Update any "TCG Market" text

7. **i18n files** (`frontend/src/i18n/locales/en.json`, `pt-BR.json`):
   Update any `"TCG Market"` string values

### Files NOT to modify
- Git repo name / remote URL
- Database table names
- Environment variable prefixes (`TCG_*`) — would break deployments
- Import paths

## Testing

- Verify: FastAPI `/docs` shows "TEDHC Market API" as title
- Verify: Frontend sidebar shows "TEDHC Market"
- Verify: Mobile header shows "TEDHC Market"
- Verify: Login page shows updated brand
- Verify: All existing tests pass (no functional changes)
- Grep: No remaining "TCG Market Intelligence" references (except git history)
