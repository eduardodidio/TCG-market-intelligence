# F141-T05: Tests for foil overlay, promo badge, and reduced-motion

**Status:** planned
**Wave:** 2

## User Story

As a developer, I want comprehensive test coverage for the new foil
holographic effect and promo badge, so regressions are caught
automatically.

## Dev Notes

### Test files to create/update

#### 1. New: `frontend/src/utils/__tests__/promo.test.ts`

Test `isPromoCard(setCode, extras)`:
- `("pone", null)` -> true (promo prefix, 4 chars)
- `("pltr", null)` -> true
- `("pmkm", null)` -> true
- `("pip", null)` -> false (3 chars, real set code)
- `("pca", null)` -> false (3 chars, real set code)
- `("fdn", null)` -> false (no promo prefix)
- `(null, null)` -> false
- `("fdn", "Foil, Promo")` -> true (extras contains "promo")
- `("fdn", "foil")` -> false (extras does not contain "promo")
- `(null, "promo pack")` -> true

#### 2. New: `frontend/src/components/__tests__/PromoBadge.test.tsx`

- Renders with `data-testid="promo-badge"`
- Contains "PROMO" text
- Has `pointer-events: none` style (or class that implies it)
- Accepts and applies optional className

#### 3. Update: `frontend/src/components/__tests__/CardPreviewModal.test.tsx`

Add tests:
- When `isPromo=true`, renders PromoBadge (`data-testid="promo-badge"`)
- When `isPromo=false` (default), does NOT render PromoBadge
- When both `isFoil=true` and `isPromo=true`, renders both foil-shimmer
  and PromoBadge
- Foil shimmer container has `foil-shimmer` class when `isFoil=true`

#### 4. Update: `frontend/src/components/__tests__/CardTile.test.tsx`

Add test:
- When card has promo set_code (e.g., `pone`), CardPreviewModal receives
  `isPromo=true` (this may require mocking the modal or checking props)

#### 5. New or update: `frontend/src/components/__tests__/Card3DTilt.test.tsx`

If this file does not exist, create it. Tests:
- When `foil=true`, renders `.foil-shimmer` wrapper
- When `foil=false`, does NOT render `.foil-shimmer` wrapper
- When `foil=true` and `disabled=true`, renders plain div (no tilt, no shimmer)
- Mouse enter adds `foil-shimmer--interactive` class
- Mouse leave removes `foil-shimmer--interactive` class
- Mouse move sets `--mouse-x` and `--mouse-y` CSS variables on the element
- When `prefers-reduced-motion: reduce` is active, no interactive class
  is added on mouse enter (mock `window.matchMedia`)

### Coverage targets

- `isPromoCard`: 100% branch coverage
- PromoBadge: 100% statement coverage
- CardPreviewModal new props: all branches covered
- Card3DTilt mouse handlers: happy path + reduced motion

### Mocking notes

- `react-parallax-tilt` is already mocked in the existing test file
  (renders a plain div with `data-testid="tilt-wrapper"`). Reuse that
  mock.
- For `prefers-reduced-motion`, mock via:
  ```ts
  vi.spyOn(window, 'matchMedia').mockReturnValue({
    matches: true, media: '', ...
  } as MediaQueryList);
  ```

## Testing

This IS the test task. Success criteria:
- All new test files pass (`cd frontend && npm test`)
- No existing tests broken
- Coverage for new code >= 90%
