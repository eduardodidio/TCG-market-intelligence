# F25-T04: Revert Page Components

- **Wave:** 1
- **Status:** planned
- **Depends on:** F25-T01
- **Type:** frontend

## User Story

As a user, I want all pages to use the consistent slate dark theme.

## Dev Notes

Replace all `tcg-*` Tailwind classes with slate equivalents in page
components:

### Pages to update:

1. **`Dashboard.tsx`** — main dashboard
2. **`MyCollection.tsx`** — collection page
3. **`Cards.tsx`** — explore cards
4. **`CardDetail.tsx`** — card detail
5. **`CollectionCardDetail.tsx`** — collection card detail
6. **`MarketMovers.tsx`** — market movers
7. **`Scans.tsx`** — price scans
8. **`Login.tsx`** — login page
9. **`DeckList.tsx`** — deck list
10. **`DeckView.tsx`** — deck detail view

### Same color mapping as T02/T03.

### Login page special attention:

Login.tsx likely has the most tcg-* classes (16 occurrences). The login form
should use:
- `bg-slate-900` for page background
- `bg-slate-800` for form card
- `border-slate-600` for inputs
- `bg-indigo-500 hover:bg-indigo-400` for login button
- `text-slate-100` for labels

### CollectionCardDetail special attention:

27 tcg-* occurrences — the most of any file. Take care to map all of them
accurately. Use the color mapping table from F25-README.

## Testing

- All existing page tests pass
- Visual check of every page route
- Login flow works correctly
- Card detail pages render all sections

## Acceptance Criteria

- [ ] No `tcg-*` classes in any page component
- [ ] All pages render with slate-900 background
- [ ] Login page styled consistently
- [ ] Card detail pages show all sections correctly
- [ ] All existing tests pass
