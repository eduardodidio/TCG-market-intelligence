# F177-T10 — banlist-sync.bat routine, README, diagram sync

- **Wave:** 3
- **Type:** docs / infra
- **Depends on:** F177-T06, F177-T07, F177-T08
- **Status:** done
- **Maps to AC:** AC5, AC11, AC12, AC13

## User Story
As the operator, I want a one-click `.bat` to refresh ban data and a README note describing F177, so that the ban list stays up to date without remembering CLI flags.

## Objective
Add `bats/banlist-sync.bat`, update `README.md` with the F177 deliverables, and re-sync the F177 diagrams with the final code.

## Dev Notes
See `_brief/00-overview.md` and `_brief/04-routing-docs-ops.md` (section F177-T10, including the full bat content).
- `bats/` is gitignored but `.bat` files are tracked: stage with `git add -f bats/banlist-sync.bat`.
- Template: `bats/process-queue.bat` (match its line endings and header style).
- The CLI command already exists: `python -m src.cli.main banlist-sync` (`src/cli/main.py:928`). Do NOT edit main.py.
- The CLI loads `.env` via `config.py` (Neon `DATABASE_URL`), so the bat writes to production directly, like `liga-sweep` (CLAUDE.md "Workflow Local → Producao").
- README: find the section where recent features are logged (search for "F170" or "F169" in `README.md`) and append the F177 note in the same style. `README.md` is a batch hotspot: append-only, small diff.
- Do NOT edit `render.yaml` or CI (CLAUDE.md guardrail).
- Diagrams: open `docs/diagrams/F177-architecture.mmd` and `F177-journey.mmd` and compare them with the merged code from T06–T09 (function names, endpoints, testids/flows). Fix any drift.

## Implementation details
1. Create `bats/banlist-sync.bat` (the content is in the brief; `exit /b 1` on errorlevel).
2. README: an F177 bullet list covering the sync fixes, the bat + Task Scheduler suggestion (daily 06:00), the `GET /api/v1/banlist` new params/fields, `GET /api/v1/banlist/status`, the card detail modal, and the removed menu item + redirect.
3. Diagram sync.

## Acceptance criteria
- [ ] `bats/banlist-sync.bat` exists, calls `python -m src.cli.main banlist-sync`, and exits 1 on failure
- [ ] The README has an F177 section with all the items above
- [ ] The diagrams match the implementation
- [ ] `pytest tests/ --cov=src --cov-report=term-missing` and `cd frontend && npm test` green (final gate); `ruff check src/` clean

## Testing
- Static check: `grep -q "banlist-sync" bats/banlist-sync.bat && grep -q "errorlevel" bats/banlist-sync.bat`.
- Optional: add `tests/banlist/test_bats.py` asserting the bat exists and contains the command + errorlevel handling (cheap regression guard, following any existing bat test if present: `grep -rn "process-queue.bat" tests`).
- Final gate: `pytest tests/ --cov=src --cov-report=term-missing`, `cd frontend && npm test`, `ruff check src/`, and `cd frontend && npm run build`.

## Test scenarios
- Happy: the bat content is correct; the README mentions `/api/v1/banlist/status` and `owned_only`
- Edge: the bat works from any cwd (`cd /d "%~dp0\.."`)
- Error: a non-zero CLI exit → the bat prints ERRO and exits 1
- Boundary: the README diff is append-only (no reflow of other features' notes)

**Test plan:** ver `F177-test-plan.md` (fixtures: bat-static-check)

## Diagrams
Owner of the final sync of `F177-architecture.mmd` + `F177-journey.mmd`.
