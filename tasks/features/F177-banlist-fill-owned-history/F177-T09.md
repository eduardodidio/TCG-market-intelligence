# F177-T09 — Remove the "Histórico de banimentos" menu item + redirect the old route

- **Wave:** 3
- **Type:** frontend
- **Depends on:** F177-T05, F177-T08
- **Status:** planned

## User Story
As a user, I want the menu to have a single ban list entry and old history links to still land somewhere useful, so that navigation is simpler and bookmarks don't break.

## Objective
Delete the nav item + the BanHistory page, and redirect `/banlist/history` → `/banlist`. This is the minimal diff on the batch hotspots `App.tsx` and `Layout.tsx`.

## Dev Notes
See `_brief/00-overview.md` (constraints) and `_brief/04-routing-docs-ops.md` (section F177-T09).
- `frontend/src/components/Layout.tsx:77`: the line `{ to: "/banlist/history", labelKey: "nav.banHistory", requiresAuth: false, icon: ICONS.clock },` → delete it. If `ICONS.clock` becomes unused, keep it anyway unless lint complains (other features may use it).
- `frontend/src/App.tsx`: `BanHistory` lazy import at ~lines 54-56; the route at ~282-290. `Navigate` is already imported (line 2). The replacement is `<Route path="/banlist/history" element={<Navigate to="/banlist" replace />} />` (no Suspense/ErrorBoundary wrapper needed; see line ~295 for the existing Navigate pattern).
- Delete: `git rm frontend/src/pages/BanHistory.tsx frontend/tests/pages/BanHistory.test.tsx`. First run `grep -rn "pages/BanHistory" frontend/src frontend/tests`; only App.tsx should reference it.
- Keep `BanEventCard.tsx`, `StatusTransition.tsx`, and the `banHistory.*` i18n keys (used elsewhere).
- `frontend/tests/components/Layout.test.tsx:368-370` references the "Ban History" link. Rewrite it to assert its absence.
- The batch F171–F179 also edits App.tsx/Layout.tsx. Rebase/merge carefully, and change only these lines.
- Gitflow: stage file by file (never `git add -A`).

## Implementation details
1. Layout.tsx: remove one nav entry.
2. App.tsx: remove the lazy import; the route element → Navigate.
3. Delete the page + its test.
4. Update Layout.test.tsx.
5. NEW `frontend/tests/routes/banlistRedirect.test.tsx`: render the App routes (or a minimal `Routes` copy is NOT acceptable; import and render `App`'s routes if the existing tests do so, otherwise mount `<MemoryRouter initialEntries={["/banlist/history"]}>` with the real App and mock the api modules) → expect `page-banlist`. If rendering the full App is too heavy, look for an existing route test pattern in `frontend/tests` and follow it.

## Acceptance criteria
- [ ] The nav has no "Ban History"/"Histórico de banimentos" link; "Ban List" is still present
- [ ] `/banlist/history` → the BanList page renders (URL `/banlist`)
- [ ] `BanHistory.tsx` and its test are removed; no dangling imports
- [ ] `cd frontend && npm test` fully green; `npm run build` OK

## Testing
Vitest + RTL. `cd frontend && npm test -- Layout banlistRedirect`, then the full suite.

## Test scenarios
- Happy: the Layout renders the Ban List link, and there is no Ban History link (en + pt-BR label)
- Happy: navigating to `/banlist/history` shows `page-banlist`
- Edge: `/banlist/history?format=modern` still redirects (query dropped is acceptable)
- Error: no console errors about the missing lazy chunk (the build passes)
- Boundary: the active-link highlight on `/banlist` still works (the existing Layout test near line 368 covered the active state; keep equivalent coverage)

## Diagrams
The journey's redirect branch (T01) must match. Report deviations to T10.
