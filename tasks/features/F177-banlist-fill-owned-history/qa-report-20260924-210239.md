# F177 — QA Validation Report

**Feature:** Ban list: populate + "Somente minha coleção" filter + history inside the card
**QA run:** 2026-09-24T21:02Z
**Scope:** Independent re-verification of the TechLead review
(`review-20260924-2030.md`, verdict `APPROVED_WITH_FOLLOWUP`), plus direct
checks of routing/menu/artifacts. Ran headless, foreground only, targeted
test paths with `--no-cov` per task instructions; did not repeat the
17m33s full-suite run TechLead already did.

## Per-criterion pass/fail

| AC | Description | Verdict | Evidence |
|----|---|---|---|
| AC1 | sync populates from JSONL/.gz/legacy array, follows redirects | PASS | `tests/banlist/test_sync_f177.py` green (part of 169-test run below); code reviewed by TechLead (`_select_download_url`, `httpx.AsyncClient(follow_redirects=True)`) |
| AC2 | Liga-style/zero-padded matching | PASS | `_build_index_keys` covers raw/mapped set codes × raw/stripped collector numbers; covered by `test_sync_f177.py` |
| AC3 | chunked upserts, 0 rows/0 history on unchanged re-run | PASS | `test_second_run_no_changes_zero_upserts_zero_history` passes |
| AC4 | compact storage policy + baseline-only first-run history | PASS | `_classify`/`should_store` logic reviewed; `test_legality_writer.py` + `test_sync_f177.py` green |
| AC5 | fail-loud on 0 matches / 0 lines (bat exits 1) | PASS (as specified) | Implemented with `MATCH_CHECK_THRESHOLD=1000` per ADR 0018, not the literal README wording — same nuance TechLead flagged; `bats/banlist-sync.bat` exits 1 on `errorlevel` — verified by reading the file directly (see below) |
| AC6 | grouped `GET /banlist`, `printings`/`owned`/`owned_quantity`, pagination, `owned_only` 401 | PASS | `tests/banlist/test_api_f177.py`, `test_api.py` green |
| AC7 | `GET /banlist/status` | PASS | covered in `test_api_f177.py` |
| AC8 | "Somente minha coleção" toggle, disabled+hint logged out, `?owned=1` | PASS | `frontend/tests/pages/BanList.test.tsx` green (part of targeted frontend run below) |
| AC9 | `BanCardDetailModal` legalities + history grouped by format | PASS | `frontend/tests/components/BanCardDetailModal.test.tsx` green |
| AC10 | nav item removed, `/banlist/history` redirects, `BanHistory.tsx` deleted | PASS | Verified directly: `grep BETA_NAV_ITEMS` in `Layout.tsx` has no ban-history entry; `App.tsx:279-280` has `<Navigate to="/banlist" replace />`; `frontend/src/pages/BanHistory.tsx` does not exist; only unrelated `BanHistorySection`/`fetchCardBanHistory` (a pre-existing, different feature in `CollectionCardDetail.tsx`) remain — no dangling references to the deleted page |
| AC11 | `bats/banlist-sync.bat` + README documents F177 | PASS | File read directly, calls `python -m src.cli.main banlist-sync`, `exit /b 1` on `errorlevel`; `README.md:1125` has "### F177 -- Ban List (2026-09-24)" section |
| AC12 | PRD, ADR 0018, both diagrams exist and match code | PASS | All four files present (`docs/prd/F177-...md`, `docs/adr/0018-...md`, `docs/diagrams/F177-architecture.mmd`, `docs/diagrams/F177-journey.mmd`); content matches implementation per TechLead's Wave 0/3 review, no drift found |
| AC13 | `pytest --cov=src` green; `npm test` green; `ruff` clean; `npm run build` OK | PASS (with known, pre-existing, out-of-scope failures) | See test output below |

## Test command output summary

- `python -m pytest tests/banlist/ --no-cov -q` → **169 passed**, 2 warnings
  (a `RuntimeWarning: coroutine ... was never awaited` from a mock in
  `test_legality_writer.py::test_empty_list_no_db_access`, and an httpx
  deprecation notice — neither indicates a real failure).
- `ruff check src/` → **All checks passed.**
- `cd frontend && npx vitest run` (targeted paths: `tests/i18n/banlist-f177-keys.test.tsx`,
  `tests/components/BanCardDetailModal.test.tsx`, `tests/pages/BanList.test.tsx`,
  `tests/api/banlist.test.ts`, `tests/routes/banlistRedirect.test.tsx`,
  `tests/components/Layout.test.tsx`, `src/components/__tests__/Layout.test.tsx`,
  `tests/pages/F97-breadcrumbs.test.tsx`) → **145/147 passed**, 1 failed file
  (`tests/components/Layout.test.tsx`, 2 failing tests). Both failures assert
  nav-link counts that include a "Card Catalog" link removed by unrelated,
  already-merged work (confirmed via `PRIMARY_NAV_ITEMS` in `Layout.tsx`
  having no such entry, and via TechLead's `git show`/`git diff` root-cause
  in the review report) — this matches the task's declared pre-existing
  baseline failure list (Layout is explicitly named) and is **not** a new
  F177 regression. F177 only ever edited `BETA_NAV_ITEMS`.
- `cd frontend && npm run build` → **succeeds**. `BanList-CW7iZjfB.js` chunk
  emitted; no `BanHistory` chunk; PWA precache (71 entries) generated
  without error.
- Full-suite runs (`pytest tests/ --cov=src`, full `npm test`) were not
  re-run in this session — TechLead already ran both to completion in the
  prior review (112 failed/4750 passed backend, 91.20% coverage; 13 failed
  files/2365 passed frontend) and cross-checked every failure against
  F177's touched-files table with zero overlap. QA independently re-ran the
  narrower, F177-relevant slices above and got consistent results, so this
  is treated as re-confirmed rather than re-run at full cost. All failures
  in both full-suite runs are pre-existing/unrelated to F177 (Liga URL
  builder, marketplace/deck/collection repositories, currency, liga-sweep
  catalog, liga-relink, provider-windows, plus the 13 named pre-existing
  frontend files) — see the task's stated baseline and TechLead's review
  for the full list.

## New tests added by QA

None. Every AC has existing, passing test coverage (T02–T10's own test
files); no gap requiring a QA-authored test was found. Cross-referenced
against `F177-test-plan.md`'s 10 test scenarios — all 10 are represented in
the current test suite.

## Live/network checks

- `bats/banlist-sync.bat` real execution against Scryfall: **pending-user**.
  Live network is blocked in this sandbox (proxy CONNECT 403 to Scryfall),
  matching the feature's own "Operational note" that the user runs the bat
  once locally post-merge (writes to Neon via `.env`) and schedules it in
  Task Scheduler. Not testable here; not a blocker per the task's own scope.
- Manual browser exercise of `/banlist` (toggle, modal, empty states):
  **pending-user** — no live app/browser session available in this headless
  run; `npm run build` succeeded and the component-level Vitest/RTL tests
  (`BanList.test.tsx`, `BanCardDetailModal.test.tsx`) exercise the same
  interactions (toggle click, modal open, badges, empty states) against
  mocked data.

## Diagrams reflect reality

Confirmed unchanged from TechLead's finding: `F177-architecture.mmd` and
`F177-journey.mmd` needed no edits after Wave 1/2 implementation (T10's own
Wave-3 pass found no drift). Not re-diffed line-by-line in this QA pass
since TechLead already verified match against the shipped code; no further
code changes have landed since that review.

## Blockers found

None. QA independently re-verified TechLead's key claims (nav-item removal,
redirect, bat exit-code behavior, README section, targeted test suites,
ruff, build) rather than trusting the review report alone, and reached the
same conclusions.

## Follow-ups (non-blocking, carried from TechLead review)

1. Backend-wide test suite has 112 pre-existing failing tests unrelated to
   any single F171–F179 feature (batch-merge fallout) — should be triaged
   before the batch reaches `main`.
2. Frontend `Layout.test.tsx` has 2 pre-existing failing assertions (stale
   "Card Catalog" nav-count expectation) that predate F177 — should be
   fixed so future real nav regressions aren't masked.
3. README-level AC5 wording is a simplification of the ADR 0018's
   documented `MATCH_CHECK_THRESHOLD` nuance — consider tightening the
   README wording in a future pass.
4. Live `bats\banlist-sync.bat` run and manual `/banlist` browser check are
   pending-user (sandbox has no live network/browser).

## Final verdict: PASSED
