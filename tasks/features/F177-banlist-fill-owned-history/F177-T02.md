# F177-T02 — i18n keys (en + pt-BR)

- **Wave:** 0
- **Type:** frontend
- **Depends on:** —
- **Status:** done
- **Maps to AC:** AC8, AC9

## User Story
As a Portuguese-speaking user, I want the new ban list controls and modal labels translated, so that the UI reads naturally ("Somente minha coleção").

## Objective
Add every `banlist.*` key needed by T05/T08 to both locale files up front, so the Wave 1/2 components render real strings in tests.

## Dev Notes
See `_brief/00-overview.md` and `_brief/03-frontend.md` (the full key table is there).
- Files: `frontend/src/i18n/locales/en.json` and `pt-BR.json`. The `"banlist": { ... }` object starts around line 671 in en.json.
- Add the keys INSIDE the existing `banlist` object; add `banlist.detail` as a nested object. Keep valid JSON (trailing commas!).
- i18next plural/interpolation: `{{count}}`, `{{date}}`.
- Do not remove `nav.banHistory` or `banHistory.*` (still used; `frontend/tests/i18n/banHistory-keys.test.tsx` checks them).
- The locale files are shared across batch features F171–F179. Keep the diff limited to the `banlist` object.
- Existing key-parity test pattern: `frontend/tests/i18n/banHistory-keys.test.tsx`, and `frontend/src/i18n/__tests__/`.

## Implementation details
- Add the 16 keys from the table in `_brief/03-frontend.md` to both files.
- NEW `frontend/tests/i18n/banlist-f177-keys.test.tsx`: loads both JSONs and asserts every new key exists in both, is non-empty, and that the pt-BR `banlist.ownedOnly === "Somente minha coleção"`.

## Acceptance criteria
- [ ] Both JSON files parse; all 16 keys exist in both
- [ ] `cd frontend && npm test -- banlist-f177-keys banHistory-keys` green
- [ ] Existing i18n parity tests (if any) stay green

## Testing
- Vitest (`cd frontend && npm test`).
- Test file at `frontend/tests/i18n/banlist-f177-keys.test.tsx`; run it with `cd frontend && npx vitest run tests/i18n/banlist-f177-keys.test.tsx`.
- No mocks; import the JSON directly.
- Regression: `cd frontend && npx vitest run tests/i18n/banHistory-keys.test.tsx` stays green.

## Test scenarios
- Happy: every key is present in en + pt-BR
- Edge: interpolation placeholders (`{{count}}`, `{{date}}`) are present in both languages
- Error: a missing key fails the test with the key name in the message
- Boundary: nested `banlist.detail.*` is resolved correctly (not flattened)

**Test plan:** ver `F177-test-plan.md` (fixtures: i18n-locale-json)

## Diagrams
None.
