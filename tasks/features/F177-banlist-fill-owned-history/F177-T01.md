# F177-T01 — PRD, ADR, diagrams, branch check

- **Wave:** 0
- **Type:** docs
- **Depends on:** —
- **Status:** done
- **Maps to AC:** AC12

## User Story
As the product owner, I want the ban list fix documented (PRD, ADR, diagrams) before code lands, so that later Waves and reviewers share one source of truth.

## Objective
Create the PRD, ADR 0018 (compact legality storage + baseline history policy), and the two mandatory Mermaid diagrams. Confirm the working branch.

## Dev Notes
See `_brief/00-overview.md` and `_brief/01-sync-fix.md`, `_brief/02-backend-api.md`, `_brief/03-frontend.md`, `_brief/04-routing-docs-ops.md` (the PRD summarizes all of them).
- PRD template: `docs/prd/template.md`. Follow the style of existing PRDs (e.g. `docs/prd/F23-deck-import.md`).
- ADRs: `docs/adr/` (last is `0013-guest-user-role-system.md`). The new one is `0018-banlist-compact-legality-storage.md`. The number 0018 is reserved for F177 in `tasks/features/EXECUTION-PLAN-F171-F179.md`; use exactly `docs/adr/0018-banlist-compact-legality-storage.md` and do not renumber.
- Diagram templates: `docs/diagrams/templates/{architecture.mmd,user-journey.mmd}`. Existing example: `docs/diagrams/F168-*.mmd`.
- Branch (Gitflow, CLAUDE.md): feature work happens on `homol` or on a worktree branch that merges into `homol`. Run `git branch --show-current`. The orchestrator worktree branch `wt/F177` is VALID (it merges into `homol`): do not switch branches. Only if it's `main`, stop and report. Never push to `main`.

## Implementation details
1. `docs/prd/F177-banlist-fill-owned-history.md`: problem, the diagnosis table (RC1–RC7), goals, non-goals (no `render.yaml` cron, no schema change), user stories, AC1–AC12, and a rollout note (run the bat once, then schedule it).
2. `docs/adr/0018-banlist-compact-legality-storage.md`: Context (2.4M rows on Neon free tier, per-row upserts), Decision (compact scope, diff-only chunked upserts, baseline history source `scryfall_baseline`), Consequences (the LegalityPanel for non-owned, non-banned cards shows "unavailable" until `scope="full"`), Alternatives (full storage; a Render cron, rejected without user confirmation).
3. `docs/diagrams/F177-architecture.mmd` — `flowchart LR`: Scryfall `/bulk-data` → `banlist_sync` (select jsonl URI → gzip stream → `_parse_bulk_line` → `_build_index_keys` match → `_classify`) → `legality_writer` → `card_legalities` / `legality_history` (Neon/SQLite). Also `bats/banlist-sync.bat` + CLI + `POST /banlist/sync` as triggers. Read side: `banlist_queries` → `GET /banlist` (owned_only) and `GET /banlist/status` → `BanList.tsx`; `/banlist/card/{id}` + `/card/{id}/history` → `BanCardDetailModal`.
4. `docs/diagrams/F177-journey.mmd` — `flowchart LR` with swimlane subgraphs User / Frontend / API. Flow: open "Lista de banimentos" → pick a format → [not synced? → the "not synced" empty state] → toggle "Somente minha coleção" → [logged out? → disabled + hint] → [no owned banned cards? → emptyOwned] → click a card → modal (legalities + history per format) → close / "Open card page". Also old URL `/banlist/history` → redirect → `/banlist`.

## Acceptance criteria
- [ ] PRD, ADR, and both `.mmd` files exist
- [ ] Both diagrams parse (valid Mermaid: `npx -y @mermaid-js/mermaid-cli` is NOT required; a syntax check by inspection is enough; no new deps)
- [ ] The branch is confirmed (not `main`; `wt/F177` is valid) and written in the PRD header

## Testing
- Docs only; no new tests. Validate with `grep -c "flowchart" docs/diagrams/F177-*.mmd` (≥1 each).
- `test -f docs/adr/0018-banlist-compact-legality-storage.md && test -f docs/prd/F177-banlist-fill-owned-history.md`.
- Regression guard: `pytest tests/ -q` and `ruff check src/` stay green (no code touched in this task).

## Test scenarios
- Happy: files exist with all sections
- Edge: ADR file must be exactly `0018-banlist-compact-legality-storage.md` (reserved number; no renumbering)
- Error: branch is `main` → stop, report, don't write code

**Test plan:** ver `F177-test-plan.md` (fixtures: none)

## Diagrams
Creates `F177-architecture.mmd` and `F177-journey.mmd` (owner).

## Notes from Developer
- Branch confirmed: `wt/F177` (worktree branch, merges into `homol`; not `main`). Recorded in the PRD header.
- `ruff check src/` is green.
- `pytest tests/ -q` (full suite, ~18 min): 112 failed / 4589 passed. All failures are in
  `tests/unit/providers/liga/test_url.py`, `tests/unit/services/test_currency.py`, and
  `tests/unit/test_liga_sweep_catalog.py` — none touch banlist, docs, or anything this task
  changed. This task made zero code changes (docs only), so these are pre-existing/environmental
  failures (this batch runs F171–F179 concurrently across worktrees), not a regression introduced
  here. `tests/banlist/` was not affected.
