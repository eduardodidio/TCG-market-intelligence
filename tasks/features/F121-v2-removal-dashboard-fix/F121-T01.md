# F121-T01: Delete V2 files and remove /v2 routes

**Status:** planned
**Wave:** 0
**Type:** frontend

## User Story

As a developer, I want to remove all V2 UI code so the codebase only contains
the classic layout, reducing complexity and dead code.

## Dev Notes

### Files to delete (8 files)
1. `frontend/src/components/LayoutV2.tsx`
2. `frontend/src/components/__tests__/LayoutV2.test.tsx`
3. `frontend/src/pages/DashboardV2.tsx`
4. `frontend/src/pages/CollectionV2.tsx`
5. `frontend/src/pages/__tests__/V2Routes.test.tsx`
6. `frontend/src/contexts/RoutePrefixContext.tsx`
7. `frontend/tests/pages/CollectionV2.test.tsx`
8. `frontend/tests/pages/DashboardV2.test.tsx`

### Files to modify

**`frontend/src/App.tsx`:**
- Remove all `/v2/*` route definitions (lines ~450-696, approximately 23 routes)
- Remove `RoutePrefixProvider` wrapper from classic layout (line ~198)
- Remove imports: `RoutePrefixProvider`, `LayoutV2`, `DashboardV2`, `CollectionV2`
  (lines 12, 111-116)
- The classic `<Layout>` should render directly without a prefix wrapper

**`frontend/src/components/Layout.tsx`:**
- Remove the "Try New UI" floating button (lines 354-361):
  ```tsx
  <Link to="/v2" className="fixed bottom-4 right-4 ..." data-testid="try-new-ui">
    Try New UI
  </Link>
  ```

### Verification
- `grep -r "V2\|v2\|RoutePrefixProvider" frontend/src/` should return no hits
  (except this task file and any unrelated occurrences like "v2" in API URLs)
- `npm run build` must succeed

## Testing

- [ ] All V2 files are deleted
- [ ] App.tsx has no V2 routes
- [ ] Frontend builds without errors
- [ ] No dangling imports reference deleted files
