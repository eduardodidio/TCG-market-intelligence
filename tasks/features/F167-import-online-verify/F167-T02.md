# F167-T02: Conditional — move to BetaRoute if issues found

**Status:** planned
**Wave:** 0

## User Story

As the platform operator, if the import feature doesn't work reliably online, I want it moved behind beta access so only testers can use it while we fix issues.

## Dev Notes

### This task is CONDITIONAL — only execute if F167-T01 reveals issues

### If issues are found, modify:

1. **`frontend/src/components/Layout.tsx`**:
   - Remove `{ to: "/import-purchases", ... }` from `PRIMARY_NAV_ITEMS`
   - Add to `BETA_NAV_ITEMS`: `{ to: "/import-purchases", labelKey: "nav.importPurchases", requiresAuth: true, icon: ICONS.arrowUpTray }`

2. **`frontend/src/App.tsx`**:
   - Wrap the `/import-purchases` route element with `<BetaRoute>`

### If all tests pass:
- No changes needed — feature stays in production nav
- Mark this task as "skipped" with note: "Tests passed, import works online"

## Testing

- If moved to beta: verify route is beta-gated (blocked for non-beta users)
- If not moved: no testing needed
