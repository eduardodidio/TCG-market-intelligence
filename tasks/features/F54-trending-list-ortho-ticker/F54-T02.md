# F54-T02 — Gaucho Orthography Fixes

**Wave:** 1 | **Status:** done | **Parallel:** yes

## User Story

As a user reading gaucho Easter egg dialogues, I want correct Portuguese orthography (accents and spelling) so the text feels authentic and polished.

## Acceptance Criteria

- [ ] All gaucho i18n strings use correct accents and spelling
- [ ] Both `en.json` and `pt-BR.json` have identical gaucho section fixes
- [ ] No code changes — i18n JSON only

## Dev Notes

### Exact fixes (both locale files, `gaucho` section):

| Key path | Current | Corrected |
|----------|---------|-----------|
| `gaucho.dashboard.message` | `"Bah meu, ta tri a plataforma ne?! Os guris se puxaram!"` | `"Bah meu, tá tri a plataforma né?! Os guris se puxaram!"` |
| `gaucho.collection.message` | `"Tche, que ta o Luxo do gaucho esta colecao?"` | `"Tchê, que tá o luxo do gaúcho esta coleção?"` |
| `gaucho.collection.opt2` | `"me caiu os butias do bolso!"` | `"me caiu os butiás do bolso!"` |
| `gaucho.collection.reply2` | `"Que barbaridade! Na proxima na rateia meu, fica experto nas trocas de realese e cards muitos roubados"` | `"Que barbaridade! Na próxima não rateia meu, fica experto nas trocas de release e cards muito roubados"` |
| `gaucho.banlist.reply1` | `"Ai sim!"` | `"Aí sim!"` |
| `gaucho.banlist.reply2` | `"Ai nao!"` | `"Aí não!"` |
| `gaucho.decks.message` | `"E este deckzinho ai? O luxo do gaucho?"` | `"E esse deckzinho aí? O luxo do gaúcho?"` |
| `gaucho.decks.opt2` | `"nao"` | `"não"` |
| `gaucho.decks.reply2` | `"ta te michando p importar o ouro aqui!?"` | `"tá te michando p importar o ouro aqui!?"` |

### Additional spelling fixes found:
- `realese` → `release` (English word, typo)
- `muitos roubados` → `muito roubados` (grammar: adverb doesn't pluralize)
- `Na proxima na rateia` → `Na próxima não rateia` (accent + typo "na" → "não")

### Files to modify
- `frontend/src/i18n/locales/en.json` (lines 543-572)
- `frontend/src/i18n/locales/pt-BR.json` (lines 543-572)

## Testing

- [ ] Snapshot/visual: verify all gaucho strings render with correct accents
- [ ] Unit: no new tests needed (string-only change), but existing gaucho tests should still pass
- [ ] Manual: trigger each gaucho dialog and verify text
