# F06-T01: Project structure, dependencies, and ADR update

**Wave:** 0
**Status:** planned
**Estimate:** S

## User Story

As a developer, I want the FastAPI project scaffolding and dependencies installed so that subsequent tasks can build endpoints on a stable foundation.

## Dev Notes

- Add runtime deps to `pyproject.toml` under `[project.dependencies]`:
  - `fastapi>=0.115`
  - `uvicorn[standard]>=0.30`
  - `pydantic>=2.7`
- Add `httpx>=0.27` to `[project.optional-dependencies] dev`
- Create the API module directory structure:
  ```
  src/api/
  src/api/__init__.py
  src/api/app.py          (empty placeholder — T06 fills this)
  src/api/deps.py          (DB session dependency injection)
  src/api/schemas/
  src/api/schemas/__init__.py
  src/api/routers/
  src/api/routers/__init__.py
  ```
- In `src/api/deps.py`, create a `get_db` dependency that yields a `Repository` instance. Follow the pattern from `src/cli/main.py` where `Repository(db_url=db)` is constructed with a configurable URL. Use an environment variable `TCG_DATABASE_URL` with fallback to `sqlite:///tcg_market.db`.
- Update `docs/adr/0002-web-stack-decision.md`: change `**Status:** proposed` to `**Status:** accepted`.
- Add a `project.scripts` entry: `api = "src.api.app:run_server"` (placeholder, will be functional after T06).

## Acceptance Criteria

- [ ] `fastapi`, `uvicorn`, `pydantic`, `httpx` are in `pyproject.toml` with pinned minimum versions
- [ ] `src/api/` directory structure exists with `__init__.py` files
- [ ] `src/api/deps.py` exports a `get_db()` function returning a `Repository`
- [ ] ADR-0002 status is `accepted`
- [ ] `pip install -e ".[dev]"` succeeds without errors
- [ ] Existing tests still pass (no regressions)

## Testing

- Unit tests: test that `get_db()` returns a `Repository` instance (in `tests/api/test_deps.py`)
- Integration tests: none (infra-only task)
